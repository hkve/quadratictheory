    tau^0 = zeros((no, no, nv, nv))

    tau^0 += einsum(
        "acik,cbkj->ijab", \lambda^2, t^2
    )

    tau^1 = zeros((no, no, nv, nv))

    tau^1 += einsum(
        "acik,kjcb->ijab", t^2, tau^0
    )

    tau^382 = zeros((no, no, no, no))

    tau^382 += 72 * einsum(
        "jlba,kiba->ijkl", tau^1, u
    )

    r^2 = zeros((nv, nv, no, no))

    r^2 += 2 * einsum(
        "ijdc,abdc->abij", tau^1, u
    )

    del tau^1

    tau^306 = zeros((no, no, nv, nv))

    tau^306 += einsum(
        "ijab->ijab", tau^0
    )

    tau^354 = zeros((no, no, nv, nv))

    tau^354 += einsum(
        "caik,kjcb->ijab", t^2, tau^0
    )

    tau^356 = zeros((no, no, nv, nv))

    tau^356 += 18 * einsum(
        "ijab->ijab", tau^354
    )

    tau^378 = zeros((no, no, nv, nv))

    tau^378 += einsum(
        "ijab->ijab", tau^354
    )

    del tau^354

    tau^389 = zeros((no, no, nv, nv))

    tau^389 -= 3 * einsum(
        "ijab->ijab", tau^0
    )

    tau^2 = zeros((no, no, no, no))

    tau^2 += einsum(
        "abij,abkl->ijkl", \lambda^2, t^2
    )

    tau^3 = zeros((no, no, nv, nv))

    tau^3 += einsum(
        "abkl,klij->ijab", t^2, tau^2
    )

    tau^235 = zeros((no, no, nv, nv))

    tau^235 += einsum(
        "ikac,kbjc->ijab", tau^3, u
    )

    tau^242 = zeros((no, no, nv, nv))

    tau^242 += einsum(
        "ijab->ijab", tau^235
    )

    del tau^235

    tau^261 = zeros((no, no, nv, nv))

    tau^261 += einsum(
        "ijab->ijab", tau^3
    )

    tau^289 = zeros((no, no, nv, nv))

    tau^289 += einsum(
        "kiac,kbjc->ijab", tau^3, u
    )

    r^2 -= 11 * einsum(
        "jiab->abij", tau^289
    ) / 18

    r^2 -= einsum(
        "jiba->abij", tau^289
    ) / 3

    del tau^289

    tau^353 = zeros((no, no, nv, nv))

    tau^353 -= 2 * einsum(
        "kibc,jkac->ijab", tau^3, u
    )

    r^2 -= einsum(
        "kjac,kbci->abij", tau^3, u
    ) / 9

    tau^76 = zeros((no, no, nv, nv))

    tau^76 += einsum(
        "ijlk,lkab->ijab", tau^2, u
    )

    tau^81 = zeros((no, no, nv, nv))

    tau^81 += 5 * einsum(
        "ijab->ijab", tau^76
    )

    tau^190 = zeros((no, no, nv, nv))

    tau^190 += einsum(
        "ijab->ijab", tau^76
    )

    tau^209 = zeros((nv, nv))

    tau^209 += einsum(
        "acij,ijbc->ab", t^2, tau^76
    )

    tau^210 = zeros((nv, nv))

    tau^210 += einsum(
        "ab->ab", tau^209
    )

    del tau^209

    tau^303 = zeros((no, no, nv, nv))

    tau^303 += einsum(
        "ijab->ijab", tau^76
    )

    del tau^76

    tau^195 = zeros((no, no, nv, nv))

    tau^195 += einsum(
        "ablk,klij->ijab", t^2, tau^2
    )

    tau^243 = zeros((no, no, nv, nv))

    tau^243 += einsum(
        "ikac,kbjc->ijab", tau^195, u
    )

    tau^254 = zeros((no, no, nv, nv))

    tau^254 += einsum(
        "ijab->ijab", tau^243
    )

    del tau^243

    tau^286 = zeros((no, no, nv, nv))

    tau^286 += einsum(
        "kiac,kbcj->ijab", tau^195, u
    )

    r^2 -= 2 * einsum(
        "jiab->abij", tau^286
    ) / 9

    r^2 += einsum(
        "jiba->abij", tau^286
    )

    del tau^286

    tau^301 = zeros((no, no, nv, nv))

    tau^301 += einsum(
        "kiac,kbjc->ijab", tau^195, u
    )

    r^2 -= 2 * einsum(
        "jiab->abij", tau^301
    ) / 9

    r^2 -= 2 * einsum(
        "jiba->abij", tau^301
    ) / 3

    del tau^301

    tau^334 = zeros((no, no, no, no))

    tau^334 += 5 * einsum(
        "ijkl->ijkl", tau^2
    )

    tau^334 -= 2 * einsum(
        "jikl->ijkl", tau^2
    )

    tau^380 = zeros((no, no, no, no))

    tau^380 += einsum(
        "ijkl->ijkl", tau^2
    )

    tau^380 += 2 * einsum(
        "jikl->ijkl", tau^2
    )

    tau^381 = zeros((no, no, no, no))

    tau^381 += 23 * einsum(
        "ijkl->ijkl", tau^2
    )

    tau^381 += 10 * einsum(
        "jikl->ijkl", tau^2
    )

    tau^396 = zeros((no, no, no, no))

    tau^396 += 2 * einsum(
        "ijkl->ijkl", tau^2
    )

    tau^396 += einsum(
        "jikl->ijkl", tau^2
    )

    tau^397 = zeros((no, no, no, no))

    tau^397 += 7 * einsum(
        "ijkl->ijkl", tau^2
    )

    tau^397 -= einsum(
        "jikl->ijkl", tau^2
    )

    tau^398 = zeros((no, no, no, no))

    tau^398 += 10 * einsum(
        "ijkl->ijkl", tau^2
    )

    tau^398 += 23 * einsum(
        "jikl->ijkl", tau^2
    )

    tau^4 = zeros((nv, nv, nv, nv))

    tau^4 += einsum(
        "abij,cdij->abcd", \lambda^2, t^2
    )

    tau^5 = zeros((no, no, nv, nv))

    tau^5 += einsum(
        "acbd,icjd->ijab", tau^4, u
    )

    tau^35 = zeros((no, no, nv, nv))

    tau^35 += einsum(
        "jiab->ijab", tau^5
    )

    tau^285 = zeros((no, no, nv, nv))

    tau^285 += einsum(
        "acki,kjcb->ijab", t^2, tau^5
    )

    del tau^5

    r^2 += 5 * einsum(
        "ijba->abij", tau^285
    ) / 9

    r^2 += einsum(
        "jiab->abij", tau^285
    )

    r^2 -= 2 * einsum(
        "jiba->abij", tau^285
    ) / 9

    del tau^285

    tau^70 = zeros((nv, nv, nv, nv))

    tau^70 += einsum(
        "aebf,cedf->abcd", tau^4, u
    )

    tau^71 = zeros((no, no, nv, nv))

    tau^71 += einsum(
        "dcij,cabd->ijab", t^2, tau^70
    )

    del tau^70

    tau^73 = zeros((no, no, nv, nv))

    tau^73 += einsum(
        "ijab->ijab", tau^71
    )

    del tau^71

    tau^123 = zeros((nv, nv, nv, nv))

    tau^123 += einsum(
        "aebf,cefd->abcd", tau^4, u
    )

    tau^125 = zeros((nv, nv, nv, nv))

    tau^125 += 5 * einsum(
        "abcd->abcd", tau^123
    )

    tau^168 = zeros((no, no, nv, nv))

    tau^168 += einsum(
        "dcij,cabd->ijab", t^2, tau^123
    )

    del tau^123

    tau^172 = zeros((no, no, nv, nv))

    tau^172 += einsum(
        "ijab->ijab", tau^168
    )

    del tau^168

    tau^287 = zeros((no, no, nv, nv))

    tau^287 += einsum(
        "acbd,icdj->ijab", tau^4, u
    )

    tau^288 = zeros((no, no, nv, nv))

    tau^288 += einsum(
        "acki,kjcb->ijab", t^2, tau^287
    )

    del tau^287

    r^2 -= 2 * einsum(
        "ijab->abij", tau^288
    ) / 9

    r^2 -= 4 * einsum(
        "ijba->abij", tau^288
    ) / 9

    r^2 -= 11 * einsum(
        "jiab->abij", tau^288
    ) / 18

    r^2 -= 2 * einsum(
        "jiba->abij", tau^288
    ) / 9

    del tau^288

    tau^6 = zeros((nv, nv, nv, nv))

    tau^6 += einsum(
        "abij,cdji->abcd", \lambda^2, t^2
    )

    tau^7 = zeros((no, no, nv, nv))

    tau^7 += einsum(
        "acbd,icdj->ijab", tau^6, u
    )

    tau^35 += einsum(
        "jiab->ijab", tau^7
    )

    tau^290 = zeros((no, no, nv, nv))

    tau^290 += einsum(
        "acki,kjcb->ijab", t^2, tau^7
    )

    del tau^7

    r^2 -= 4 * einsum(
        "ijab->abij", tau^290
    ) / 9

    r^2 -= 2 * einsum(
        "ijba->abij", tau^290
    ) / 9

    r^2 -= 2 * einsum(
        "jiab->abij", tau^290
    ) / 9

    r^2 -= 11 * einsum(
        "jiba->abij", tau^290
    ) / 18

    del tau^290

    tau^150 = zeros((nv, nv, nv, nv))

    tau^150 += einsum(
        "aebf,cefd->abcd", tau^6, u
    )

    tau^151 = zeros((nv, nv, nv, nv))

    tau^151 += 5 * einsum(
        "abcd->abcd", tau^150
    )

    tau^173 = zeros((no, no, nv, nv))

    tau^173 += einsum(
        "dcij,cabd->ijab", t^2, tau^150
    )

    del tau^150

    tau^175 = zeros((no, no, nv, nv))

    tau^175 += einsum(
        "ijab->ijab", tau^173
    )

    del tau^173

    tau^180 = zeros((no, no, nv, nv))

    tau^180 += einsum(
        "acbd,icjd->ijab", tau^6, u
    )

    tau^184 = zeros((no, no, nv, nv))

    tau^184 -= 2 * einsum(
        "ijab->ijab", tau^180
    )

    del tau^180

    tau^321 = zeros((nv, nv, nv, nv))

    tau^321 -= einsum(
        "aecf,bedf->abcd", tau^6, u
    )

    tau^8 = zeros((no, no, nv, nv))

    tau^8 += einsum(
        "acik,jcbk->ijab", \lambda^2, u
    )

    tau^21 = zeros((no, no, nv, nv))

    tau^21 += einsum(
        "ijab->ijab", tau^8
    )

    tau^23 = zeros((no, no, nv, nv))

    tau^23 += einsum(
        "ijab->ijab", tau^8
    )

    tau^45 = zeros((no, no, nv, nv))

    tau^45 -= einsum(
        "ijab->ijab", tau^8
    )

    tau^198 = zeros((no, no, nv, nv))

    tau^198 += einsum(
        "ijab->ijab", tau^8
    )

    tau^212 = zeros((no, no, nv, nv))

    tau^212 += einsum(
        "ijab->ijab", tau^8
    )

    tau^374 = zeros((no, no, nv, nv))

    tau^374 -= 3 * einsum(
        "ijab->ijab", tau^8
    )

    tau^404 = zeros((no, no, nv, nv))

    tau^404 -= einsum(
        "ijab->ijab", tau^8
    )

    del tau^8

    tau^9 = zeros((no, no, nv, nv))

    tau^9 += einsum(
        "acki,jckb->ijab", \lambda^2, u
    )

    tau^21 += einsum(
        "ijab->ijab", tau^9
    )

    tau^23 += einsum(
        "ijab->ijab", tau^9
    )

    tau^212 += einsum(
        "ijab->ijab", tau^9
    )

    tau^282 = zeros((no, no, nv, nv))

    tau^282 += einsum(
        "ijab->ijab", tau^9
    )

    tau^391 = zeros((no, no, nv, nv))

    tau^391 -= einsum(
        "ijab->ijab", tau^9
    )

    tau^406 = zeros((no, no, nv, nv))

    tau^406 += einsum(
        "ijab->ijab", tau^9
    )

    tau^413 = zeros((no, no, nv, nv))

    tau^413 += einsum(
        "ijab->ijab", tau^9
    )

    del tau^9

    tau^10 = zeros((no, no, nv, nv))

    tau^10 += einsum(
        "acki,kjbc->ijab", t^2, u
    )

    tau^11 = zeros((no, no, nv, nv))

    tau^11 += einsum(
        "caik,kjcb->ijab", \lambda^2, tau^10
    )

    tau^21 -= einsum(
        "ijab->ijab", tau^11
    )

    tau^23 -= einsum(
        "ijab->ijab", tau^11
    )

    tau^212 -= einsum(
        "ijab->ijab", tau^11
    )

    tau^282 -= einsum(
        "ijab->ijab", tau^11
    )

    tau^283 = zeros((no, no, nv, nv))

    tau^283 += einsum(
        "bckj,kiac->ijab", t^2, tau^282
    )

    del tau^282

    tau^284 = zeros((no, no, nv, nv))

    tau^284 += einsum(
        "bckj,kica->ijab", t^2, tau^283
    )

    del tau^283

    r^2 -= 11 * einsum(
        "ijab->abij", tau^284
    ) / 18

    r^2 -= 2 * einsum(
        "ijba->abij", tau^284
    ) / 9

    r^2 -= 2 * einsum(
        "jiab->abij", tau^284
    ) / 9

    r^2 -= 4 * einsum(
        "jiba->abij", tau^284
    ) / 9

    del tau^284

    tau^374 -= einsum(
        "ijab->ijab", tau^11
    )

    tau^391 += einsum(
        "ijab->ijab", tau^11
    )

    tau^406 -= einsum(
        "ijab->ijab", tau^11
    )

    del tau^11

    tau^217 = zeros((no, no, nv, nv))

    tau^217 += einsum(
        "ijab->ijab", tau^10
    )

    tau^256 = zeros((no, no, nv, nv))

    tau^256 += einsum(
        "caki,kjcb->ijab", \lambda^2, tau^10
    )

    tau^257 = zeros((no, no, nv, nv))

    tau^257 -= einsum(
        "ijab->ijab", tau^256
    )

    tau^279 = zeros((no, no, nv, nv))

    tau^279 -= einsum(
        "ijab->ijab", tau^256
    )

    tau^405 = zeros((no, no, nv, nv))

    tau^405 += einsum(
        "ijab->ijab", tau^256
    )

    del tau^256

    tau^269 = zeros((no, no, nv, nv))

    tau^269 += einsum(
        "ikac,kjbc->ijab", tau^10, tau^3
    )

    r^2 += einsum(
        "ijab->abij", tau^269
    ) / 3

    r^2 += 11 * einsum(
        "ijba->abij", tau^269
    ) / 18

    del tau^269

    tau^270 = zeros((no, no, nv, nv))

    tau^270 += einsum(
        "jkac,ikbc->ijab", tau^10, tau^3
    )

    r^2 += einsum(
        "ijab->abij", tau^270
    ) / 3

    r^2 += 4 * einsum(
        "ijba->abij", tau^270
    ) / 9

    del tau^270

    tau^276 = zeros((no, no, nv, nv))

    tau^276 += einsum(
        "ikac,kjbc->ijab", tau^10, tau^195
    )

    tau^278 = zeros((no, no, nv, nv))

    tau^278 += einsum(
        "ijab->ijab", tau^276
    )

    del tau^276

    tau^277 = zeros((no, no, nv, nv))

    tau^277 += einsum(
        "jkac,ikbc->ijab", tau^10, tau^195
    )

    tau^278 += einsum(
        "ijab->ijab", tau^277
    )

    del tau^277

    r^2 += 2 * einsum(
        "ijab->abij", tau^278
    ) / 3

    r^2 += 2 * einsum(
        "ijba->abij", tau^278
    ) / 9

    del tau^278

    tau^312 = zeros((no, no, nv, nv))

    tau^312 += einsum(
        "ijcd,acbd->ijab", tau^10, tau^4
    )

    tau^12 = zeros((nv, nv))

    tau^12 += einsum(
        "caij,cbij->ab", \lambda^2, t^2
    )

    tau^13 = zeros((no, no, nv, nv))

    tau^13 += einsum(
        "ac,ijbc->ijab", tau^12, u
    )

    tau^21 += 2 * einsum(
        "ijab->ijab", tau^13
    )

    tau^21 -= einsum(
        "jiab->ijab", tau^13
    )

    tau^23 -= einsum(
        "ijba->ijab", tau^13
    )

    tau^23 += 2 * einsum(
        "jiba->ijab", tau^13
    )

    tau^204 = zeros((no, no, nv, nv))

    tau^204 += einsum(
        "ijab->ijab", tau^13
    )

    tau^212 += 2 * einsum(
        "ijab->ijab", tau^13
    )

    tau^212 -= einsum(
        "jiab->ijab", tau^13
    )

    tau^374 += 3 * einsum(
        "jiab->ijab", tau^13
    )

    tau^377 = zeros((no, no, nv, nv))

    tau^377 += 3 * einsum(
        "ijab->ijab", tau^13
    )

    tau^404 += einsum(
        "ijba->ijab", tau^13
    )

    tau^405 += einsum(
        "jiba->ijab", tau^13
    )

    tau^406 += 2 * einsum(
        "jiba->ijab", tau^13
    )

    del tau^13

    tau^411 = zeros((no, no))

    tau^411 -= 9 * einsum(
        "abik,kjab->ij", t^2, tau^406
    )

    del tau^406

    tau^47 = zeros((no, no, nv, nv))

    tau^47 += einsum(
        "cb,acij->ijab", tau^12, t^2
    )

    tau^53 = zeros((no, no, nv, nv))

    tau^53 -= 2 * einsum(
        "ijab->ijab", tau^47
    )

    tau^63 = zeros((no, no, nv, nv))

    tau^63 += einsum(
        "ijdc,abdc->ijab", tau^47, u
    )

    tau^69 = zeros((no, no, nv, nv))

    tau^69 += einsum(
        "ijab->ijab", tau^63
    )

    del tau^63

    tau^208 = zeros((nv, nv))

    tau^208 += einsum(
        "jiac,jicb->ab", tau^47, u
    )

    del tau^47

    tau^210 += einsum(
        "ab->ab", tau^208
    )

    del tau^208

    tau^211 = zeros((no, no, nv, nv))

    tau^211 += einsum(
        "ac,bcij->ijab", tau^210, t^2
    )

    del tau^210

    r^2 -= einsum(
        "ijba->abij", tau^211
    )

    r^2 += einsum(
        "ijab->abij", tau^211
    ) / 9

    r^2 -= 7 * einsum(
        "jiab->abij", tau^211
    ) / 9

    del tau^211

    tau^201 = zeros((no, no, nv, nv))

    tau^201 += einsum(
        "ac,ibjc->ijab", tau^12, u
    )

    tau^206 = zeros((no, no, nv, nv))

    tau^206 += einsum(
        "ijab->ijab", tau^201
    )

    del tau^201

    tau^356 += 14 * einsum(
        "ca,cbij->ijab", tau^12, t^2
    )

    tau^14 = zeros((no, no))

    tau^14 += einsum(
        "abki,abkj->ij", \lambda^2, t^2
    )

    tau^15 = zeros((no, no, nv, nv))

    tau^15 += einsum(
        "ik,kjba->ijab", tau^14, u
    )

    tau^21 += 2 * einsum(
        "ijab->ijab", tau^15
    )

    tau^21 -= einsum(
        "ijba->ijab", tau^15
    )

    tau^21 -= einsum(
        "jiab->ijab", tau^15
    )

    tau^21 += 2 * einsum(
        "jiba->ijab", tau^15
    )

    tau^23 += 2 * einsum(
        "ijab->ijab", tau^15
    )

    tau^23 -= einsum(
        "ijba->ijab", tau^15
    )

    tau^45 += einsum(
        "jiab->ijab", tau^15
    )

    tau^204 += einsum(
        "ijab->ijab", tau^15
    )

    tau^205 = zeros((no, no, nv, nv))

    tau^205 += einsum(
        "bckj,ikac->ijab", t^2, tau^204
    )

    del tau^204

    tau^206 -= einsum(
        "ijab->ijab", tau^205
    )

    del tau^205

    tau^405 += einsum(
        "ijab->ijab", tau^15
    )

    tau^405 -= 2 * einsum(
        "ijba->ijab", tau^15
    )

    tau^413 -= einsum(
        "ijba->ijab", tau^15
    )

    del tau^15

    tau^414 = zeros((no, no))

    tau^414 -= einsum(
        "bakj,kiab->ij", t^2, tau^413
    )

    del tau^413

    tau^203 = zeros((no, no, nv, nv))

    tau^203 += einsum(
        "ik,kajb->ijab", tau^14, u
    )

    tau^206 += einsum(
        "ijba->ijab", tau^203
    )

    del tau^203

    tau^412 = zeros((no, no, nv, nv))

    tau^412 -= 2 * einsum(
        "kj,abki->ijab", tau^14, t^2
    )

    tau^16 = zeros((no, no, nv, nv))

    tau^16 += einsum(
        "acki,kjcb->ijab", t^2, u
    )

    tau^19 = zeros((no, no, nv, nv))

    tau^19 += einsum(
        "ijab->ijab", tau^16
    )

    tau^38 = zeros((no, no, nv, nv))

    tau^38 += einsum(
        "ijab->ijab", tau^16
    )

    del tau^16

    tau^17 = zeros((no, no, nv, nv))

    tau^17 -= einsum(
        "jiab->ijab", u
    )

    tau^17 += 2 * einsum(
        "jiba->ijab", u
    )

    tau^18 = zeros((no, no, nv, nv))

    tau^18 += einsum(
        "bcjk,kica->ijab", t^2, tau^17
    )

    tau^19 -= einsum(
        "jiba->ijab", tau^18
    )

    tau^20 = zeros((no, no, nv, nv))

    tau^20 += einsum(
        "cbkj,kica->ijab", \lambda^2, tau^19
    )

    tau^21 -= einsum(
        "jiba->ijab", tau^20
    )

    tau^22 = zeros((no, no, nv, nv))

    tau^22 += einsum(
        "bckj,kiac->ijab", t^2, tau^21
    )

    del tau^21

    tau^35 += einsum(
        "jiab->ijab", tau^22
    )

    del tau^22

    tau^23 -= einsum(
        "jiba->ijab", tau^20
    )

    tau^24 = zeros((no, no, nv, nv))

    tau^24 += einsum(
        "bcjk,ikca->ijab", t^2, tau^23
    )

    del tau^23

    tau^35 -= 2 * einsum(
        "jiab->ijab", tau^24
    )

    del tau^24

    tau^45 += einsum(
        "jiba->ijab", tau^20
    )

    tau^198 -= einsum(
        "jiba->ijab", tau^20
    )

    tau^199 = zeros((no, no, nv, nv))

    tau^199 += einsum(
        "bckj,kiac->ijab", t^2, tau^198
    )

    del tau^198

    tau^200 = zeros((no, no, nv, nv))

    tau^200 += einsum(
        "bckj,kica->ijab", t^2, tau^199
    )

    del tau^199

    r^2 -= 2 * einsum(
        "ijab->abij", tau^200
    ) / 9

    r^2 += einsum(
        "ijba->abij", tau^200
    )

    r^2 += 5 * einsum(
        "jiab->abij", tau^200
    ) / 9

    del tau^200

    tau^212 -= einsum(
        "jiba->ijab", tau^20
    )

    tau^213 = zeros((no, no, nv, nv))

    tau^213 += einsum(
        "bcjk,kiac->ijab", t^2, tau^212
    )

    del tau^212

    tau^214 = zeros((no, no, nv, nv))

    tau^214 += einsum(
        "bckj,kica->ijab", t^2, tau^213
    )

    del tau^213

    r^2 += einsum(
        "ijab->abij", tau^214
    )

    r^2 -= 2 * einsum(
        "ijba->abij", tau^214
    ) / 9

    r^2 += 5 * einsum(
        "jiba->abij", tau^214
    ) / 9

    del tau^214

    tau^404 += einsum(
        "jiba->ijab", tau^20
    )

    del tau^20

    tau^77 = zeros((no, no, nv, nv))

    tau^77 += einsum(
        "cbjk,kica->ijab", \lambda^2, tau^19
    )

    tau^81 += 2 * einsum(
        "jiba->ijab", tau^77
    )

    tau^257 -= einsum(
        "jiba->ijab", tau^77
    )

    del tau^77

    tau^196 = zeros((no, no, nv, nv))

    tau^196 += einsum(
        "ikac,kjbc->ijab", tau^19, tau^195
    )

    del tau^19

    del tau^195

    tau^197 = zeros((no, no, nv, nv))

    tau^197 += einsum(
        "ijab->ijab", tau^196
    )

    del tau^196

    tau^144 = zeros((no, no, nv, nv))

    tau^144 -= einsum(
        "jiba->ijab", tau^18
    )

    tau^372 = zeros((no, no, nv, nv))

    tau^372 -= 3 * einsum(
        "jiba->ijab", tau^18
    )

    del tau^18

    tau^343 = zeros((no, no, nv, nv))

    tau^343 += einsum(
        "acki,kjbc->ijab", t^2, tau^17
    )

    tau^393 = zeros((no, no, nv, nv))

    tau^393 += 2 * einsum(
        "caki,kjcb->ijab", t^2, tau^17
    )

    tau^400 = zeros((no, no, nv, nv))

    tau^400 -= 2 * einsum(
        "kicb,kjac->ijab", tau^17, tau^3
    )

    del tau^17

    del tau^3

    tau^25 = zeros((no, no, nv, nv))

    tau^25 += einsum(
        "caik,bckj->ijab", \lambda^2, t^2
    )

    tau^27 = zeros((no, no, nv, nv))

    tau^27 += einsum(
        "ijab->ijab", tau^25
    )

    tau^64 = zeros((no, no, nv, nv))

    tau^64 += einsum(
        "ijcd,acbd->ijab", tau^25, u
    )

    tau^67 = zeros((no, no, nv, nv))

    tau^67 += einsum(
        "ijab->ijab", tau^64
    )

    del tau^64

    tau^103 = zeros((no, no, nv, nv))

    tau^103 += einsum(
        "ijcd,acdb->ijab", tau^25, u
    )

    tau^104 = zeros((no, no, nv, nv))

    tau^104 += einsum(
        "acki,kjbc->ijab", t^2, tau^103
    )

    del tau^103

    tau^108 = zeros((no, no, nv, nv))

    tau^108 += einsum(
        "ijab->ijab", tau^104
    )

    del tau^104

    tau^109 = zeros((no, no, nv, nv))

    tau^109 += einsum(
        "cbik,kjca->ijab", t^2, tau^25
    )

    tau^110 = zeros((no, no, nv, nv))

    tau^110 += einsum(
        "ikac,kbjc->ijab", tau^109, u
    )

    tau^148 = zeros((no, no, nv, nv))

    tau^148 -= 6 * einsum(
        "ijab->ijab", tau^110
    )

    del tau^110

    tau^163 = zeros((no, no, nv, nv))

    tau^163 += einsum(
        "ikac,jkcb->ijab", tau^109, u
    )

    del tau^109

    tau^165 = zeros((no, no, nv, nv))

    tau^165 += einsum(
        "jiab->ijab", tau^163
    )

    del tau^163

    tau^128 = zeros((no, no, nv, nv))

    tau^128 += einsum(
        "acki,kjcb->ijab", t^2, tau^25
    )

    tau^129 = zeros((no, no, nv, nv))

    tau^129 += einsum(
        "ikca,jkcb->ijab", tau^128, u
    )

    tau^136 = zeros((no, no, nv, nv))

    tau^136 += 3 * einsum(
        "ijba->ijab", tau^129
    )

    del tau^129

    tau^178 = zeros((no, no, nv, nv))

    tau^178 += einsum(
        "klab,lkji->ijab", tau^128, u
    )

    tau^186 = zeros((no, no, nv, nv))

    tau^186 += 6 * einsum(
        "ijab->ijab", tau^178
    )

    del tau^178

    tau^300 = zeros((no, no, nv, nv))

    tau^300 += einsum(
        "ikca,kbjc->ijab", tau^128, u
    )

    r^2 -= einsum(
        "jiab->abij", tau^300
    ) / 3

    r^2 -= 2 * einsum(
        "jiba->abij", tau^300
    ) / 3

    del tau^300

    tau^401 = zeros((no, no, nv, nv))

    tau^401 += einsum(
        "ijba->ijab", tau^128
    )

    del tau^128

    tau^156 = zeros((no, no, nv, nv))

    tau^156 += einsum(
        "jkcb,ikca->ijab", tau^10, tau^25
    )

    tau^160 = zeros((no, no, nv, nv))

    tau^160 += 3 * einsum(
        "jiba->ijab", tau^156
    )

    del tau^156

    tau^179 = zeros((no, no, nv, nv))

    tau^179 += einsum(
        "klab,lijk->ijab", tau^25, u
    )

    tau^184 += 5 * einsum(
        "ijab->ijab", tau^179
    )

    del tau^179

    tau^291 = zeros((no, no, nv, nv))

    tau^291 += einsum(
        "ikca,kcjb->ijab", tau^25, u
    )

    tau^293 = zeros((no, no, nv, nv))

    tau^293 += einsum(
        "ijab->ijab", tau^291
    )

    del tau^291

    tau^295 = zeros((no, no, nv, nv))

    tau^295 += einsum(
        "ikca,kcbj->ijab", tau^25, u
    )

    tau^297 = zeros((no, no, nv, nv))

    tau^297 += einsum(
        "ijab->ijab", tau^295
    )

    del tau^295

    tau^26 = zeros((no, no, nv, nv))

    tau^26 += einsum(
        "caki,bcjk->ijab", \lambda^2, t^2
    )

    tau^27 += einsum(
        "ijab->ijab", tau^26
    )

    tau^28 = zeros((no, no, nv, nv))

    tau^28 += einsum(
        "jkcb,kcai->ijab", tau^27, u
    )

    tau^35 += einsum(
        "ijab->ijab", tau^28
    )

    del tau^28

    tau^55 = zeros((nv, nv))

    tau^55 += einsum(
        "jicb,icja->ab", tau^27, u
    )

    tau^60 = zeros((nv, nv))

    tau^60 += einsum(
        "ba->ab", tau^55
    )

    del tau^55

    tau^176 = zeros((no, no, nv, nv))

    tau^176 += einsum(
        "klab,likj->ijab", tau^27, u
    )

    tau^177 = zeros((no, no, nv, nv))

    tau^177 += einsum(
        "bckj,kica->ijab", t^2, tau^176
    )

    del tau^176

    r^2 -= 7 * einsum(
        "jiab->abij", tau^177
    ) / 9

    r^2 += einsum(
        "ijab->abij", tau^177
    ) / 9

    del tau^177

    tau^37 = zeros((no, no, nv, nv))

    tau^37 += einsum(
        "ikca,kcjb->ijab", tau^26, u
    )

    tau^40 = zeros((no, no, nv, nv))

    tau^40 += einsum(
        "ijab->ijab", tau^37
    )

    del tau^37

    tau^202 = zeros((no, no, nv, nv))

    tau^202 += einsum(
        "ikca,kcbj->ijab", tau^26, u
    )

    tau^206 += einsum(
        "ijba->ijab", tau^202
    )

    del tau^202

    tau^207 = zeros((no, no, nv, nv))

    tau^207 += einsum(
        "bckj,kica->ijab", t^2, tau^206
    )

    del tau^206

    r^2 += einsum(
        "jiab->abij", tau^207
    )

    r^2 += einsum(
        "ijba->abij", tau^207
    )

    del tau^207

    tau^324 = zeros((no, no, nv, nv))

    tau^324 += 9 * einsum(
        "ijab->ijab", tau^26
    )

    tau^365 = zeros((no, no, nv, nv))

    tau^365 += 18 * einsum(
        "jkca,ikcb->ijab", tau^10, tau^26
    )

    tau^368 = zeros((no, no, nv, nv))

    tau^368 -= 3 * einsum(
        "ijab->ijab", tau^26
    )

    tau^387 = zeros((no, no, nv, nv))

    tau^387 -= 36 * einsum(
        "ijab->ijab", tau^26
    )

    tau^388 = zeros((no, no, nv, nv))

    tau^388 -= 36 * einsum(
        "klab,lijk->ijab", tau^26, u
    )

    tau^388 -= 36 * einsum(
        "ijcd,bcda->ijab", tau^26, u
    )

    tau^29 = zeros((no, no, nv, nv))

    tau^29 -= einsum(
        "abij->ijab", t^2
    )

    tau^29 += 2 * einsum(
        "abji->ijab", t^2
    )

    tau^30 = zeros((no, no, nv, nv))

    tau^30 += einsum(
        "cbkj,kiac->ijab", \lambda^2, tau^29
    )

    tau^31 = zeros((no, no, nv, nv))

    tau^31 += einsum(
        "kjbc,kcia->ijab", tau^30, u
    )

    tau^35 -= einsum(
        "ijab->ijab", tau^31
    )

    del tau^31

    tau^312 -= einsum(
        "ikca,kjbc->ijab", tau^10, tau^30
    )

    tau^331 = zeros((no, no, nv, nv))

    tau^331 += einsum(
        "bcjk,ikac->ijab", t^2, tau^30
    )

    tau^332 = zeros((no, no, nv, nv))

    tau^332 -= einsum(
        "ijab->ijab", tau^331
    )

    del tau^331

    tau^348 = zeros((no, no, nv, nv))

    tau^348 += einsum(
        "cbkj,ikac->ijab", t^2, tau^30
    )

    tau^349 = zeros((no, no, nv, nv))

    tau^349 -= einsum(
        "ijab->ijab", tau^348
    )

    del tau^348

    tau^311 = zeros((no, no, nv, nv))

    tau^311 += einsum(
        "acik,kjbc->ijab", \lambda^2, tau^29
    )

    tau^317 = zeros((no, no, nv, nv))

    tau^317 += einsum(
        "kiac,kjcb->ijab", tau^29, u
    )

    tau^318 = zeros((no, no, nv, nv))

    tau^318 += einsum(
        "jiab->ijab", tau^317
    )

    del tau^317

    tau^32 = zeros((no, no, nv, nv))

    tau^32 += 2 * einsum(
        "iabj->ijab", u
    )

    tau^32 -= einsum(
        "iajb->ijab", u
    )

    tau^33 = zeros((no, no, nv, nv))

    tau^33 += einsum(
        "bc,ijac->ijab", tau^12, tau^32
    )

    tau^35 -= einsum(
        "jiba->ijab", tau^33
    )

    del tau^33

    tau^34 = zeros((no, no, nv, nv))

    tau^34 += einsum(
        "jk,kiab->ijab", tau^14, tau^32
    )

    del tau^32

    tau^35 -= einsum(
        "ijba->ijab", tau^34
    )

    del tau^34

    tau^36 = zeros((no, no, nv, nv))

    tau^36 += einsum(
        "bcjk,ikca->ijab", t^2, tau^35
    )

    del tau^35

    tau^62 = zeros((no, no, nv, nv))

    tau^62 += einsum(
        "jiba->ijab", tau^36
    )

    del tau^36

    tau^38 -= einsum(
        "jabi->ijab", u
    )

    tau^39 = zeros((no, no, nv, nv))

    tau^39 += einsum(
        "jk,ikab->ijab", tau^14, tau^38
    )

    tau^40 -= einsum(
        "jiab->ijab", tau^39
    )

    del tau^39

    tau^41 = zeros((no, no, nv, nv))

    tau^41 += einsum(
        "bckj,kiac->ijab", t^2, tau^40
    )

    del tau^40

    tau^62 += einsum(
        "jiba->ijab", tau^41
    )

    del tau^41

    tau^124 = zeros((nv, nv, nv, nv))

    tau^124 += einsum(
        "ijcd,ijab->abcd", tau^25, tau^38
    )

    tau^125 += 6 * einsum(
        "cdab->abcd", tau^124
    )

    tau^126 = zeros((no, no, nv, nv))

    tau^126 += einsum(
        "cdij,cabd->ijab", t^2, tau^125
    )

    del tau^125

    tau^148 += einsum(
        "ijab->ijab", tau^126
    )

    del tau^126

    tau^154 = zeros((nv, nv, nv, nv))

    tau^154 += 6 * einsum(
        "cdab->abcd", tau^124
    )

    del tau^124

    tau^215 = zeros((no, no, nv, nv))

    tau^215 += einsum(
        "bc,ijac->ijab", tau^12, tau^38
    )

    del tau^38

    tau^216 = zeros((no, no, nv, nv))

    tau^216 -= einsum(
        "bckj,ikac->ijab", t^2, tau^215
    )

    del tau^215

    r^2 += 5 * einsum(
        "jiba->abij", tau^216
    ) / 9

    r^2 -= 2 * einsum(
        "ijba->abij", tau^216
    ) / 9

    r^2 += einsum(
        "ijab->abij", tau^216
    )

    del tau^216

    tau^42 = zeros((no, no, nv, nv))

    tau^42 += einsum(
        "cdij,dcba->ijab", \lambda^2, u
    )

    tau^43 = zeros((nv, nv))

    tau^43 += einsum(
        "acij,ijbc->ab", t^2, tau^42
    )

    tau^60 -= einsum(
        "ab->ab", tau^43
    )

    del tau^43

    tau^81 += 6 * einsum(
        "ijab->ijab", tau^42
    )

    tau^92 = zeros((no, no, nv, nv))

    tau^92 += 3 * einsum(
        "jiab->ijab", tau^42
    )

    tau^303 += einsum(
        "ijab->ijab", tau^42
    )

    del tau^42

    tau^44 = zeros((no, no, nv, nv))

    tau^44 += 2 * einsum(
        "abij->ijab", t^2
    )

    tau^44 -= einsum(
        "abji->ijab", t^2
    )

    tau^46 = zeros((nv, nv))

    tau^46 += einsum(
        "ijac,jicb->ab", tau^44, tau^45
    )

    del tau^45

    tau^60 += einsum(
        "ab->ab", tau^46
    )

    del tau^46

    tau^52 = zeros((no, no, nv, nv))

    tau^52 += einsum(
        "kj,ikab->ijab", tau^14, tau^44
    )

    tau^53 -= einsum(
        "ijab->ijab", tau^52
    )

    del tau^52

    tau^115 = zeros((no, no, nv, nv))

    tau^115 += einsum(
        "ikac,kjcb->ijab", tau^44, u
    )

    tau^116 = zeros((no, no, nv, nv))

    tau^116 += einsum(
        "ijab->ijab", tau^115
    )

    del tau^115

    tau^48 = zeros((no, no, nv, nv))

    tau^48 += einsum(
        "acik,bcjk->ijab", \lambda^2, t^2
    )

    tau^50 = zeros((no, no, nv, nv))

    tau^50 += einsum(
        "ijab->ijab", tau^48
    )

    tau^340 = zeros((no, no, nv, nv))

    tau^340 += einsum(
        "ijab->ijab", tau^48
    )

    tau^364 = zeros((no, no, nv, nv))

    tau^364 -= 3 * einsum(
        "ijab->ijab", tau^48
    )

    tau^365 += 18 * einsum(
        "klab,lijk->ijab", tau^48, u
    )

    tau^403 = zeros((no, no, nv, nv))

    tau^403 += einsum(
        "ikac,kjcb->ijab", tau^44, tau^48
    )

    del tau^44

    tau^49 = zeros((no, no, nv, nv))

    tau^49 += einsum(
        "acki,bckj->ijab", \lambda^2, t^2
    )

    tau^50 += einsum(
        "ijab->ijab", tau^49
    )

    tau^51 = zeros((no, no, nv, nv))

    tau^51 += einsum(
        "cbjk,kica->ijab", t^2, tau^50
    )

    tau^53 += einsum(
        "jiab->ijab", tau^51
    )

    del tau^51

    tau^54 = zeros((nv, nv))

    tau^54 += einsum(
        "ijac,ijbc->ab", tau^53, u
    )

    del tau^53

    tau^60 -= einsum(
        "ab->ab", tau^54
    )

    del tau^54

    tau^74 = zeros((no, no, nv, nv))

    tau^74 += einsum(
        "klab,lijk->ijab", tau^49, u
    )

    tau^83 = zeros((no, no, nv, nv))

    tau^83 += 6 * einsum(
        "ijab->ijab", tau^74
    )

    del tau^74

    tau^306 += einsum(
        "ijab->ijab", tau^49
    )

    tau^307 = zeros((no, no, nv, nv))

    tau^307 += einsum(
        "bckj,kica->ijab", t^2, tau^306
    )

    tau^308 = zeros((no, no, nv, nv))

    tau^308 += einsum(
        "jiba->ijab", tau^307
    )

    del tau^307

    tau^412 += einsum(
        "cbjk,kica->ijab", t^2, tau^306
    )

    tau^56 = zeros((nv, nv, nv, nv))

    tau^56 -= einsum(
        "bacd->abcd", u
    )

    tau^56 += 2 * einsum(
        "badc->abcd", u
    )

    tau^57 = zeros((nv, nv))

    tau^57 += einsum(
        "cd,cadb->ab", tau^12, tau^56
    )

    tau^60 += einsum(
        "ab->ab", tau^57
    )

    del tau^57

    tau^58 = zeros((no, no, nv, nv))

    tau^58 -= einsum(
        "iabj->ijab", u
    )

    tau^58 += 2 * einsum(
        "iajb->ijab", u
    )

    tau^59 = zeros((nv, nv))

    tau^59 += einsum(
        "ji,ijab->ab", tau^14, tau^58
    )

    tau^60 -= einsum(
        "ab->ab", tau^59
    )

    del tau^59

    tau^61 = zeros((no, no, nv, nv))

    tau^61 += einsum(
        "ac,bcij->ijab", tau^60, t^2
    )

    del tau^60

    tau^62 += einsum(
        "ijba->ijab", tau^61
    )

    del tau^61

    r^2 += einsum(
        "ijab->abij", tau^62
    )

    r^2 += einsum(
        "jiba->abij", tau^62
    )

    del tau^62

    tau^408 = zeros((no, no))

    tau^408 += einsum(
        "ab,ijab->ij", tau^12, tau^58
    )

    del tau^58

    tau^411 += 9 * einsum(
        "ji->ij", tau^408
    )

    tau^414 += einsum(
        "ij->ij", tau^408
    )

    del tau^408

    tau^65 = zeros((nv, nv, nv, nv))

    tau^65 += einsum(
        "abji,jicd->abcd", t^2, u
    )

    tau^66 = zeros((no, no, nv, nv))

    tau^66 += einsum(
        "ijcd,acbd->ijab", tau^0, tau^65
    )

    tau^67 += einsum(
        "ijab->ijab", tau^66
    )

    del tau^66

    tau^68 = zeros((no, no, nv, nv))

    tau^68 += einsum(
        "bcjk,kiac->ijab", t^2, tau^67
    )

    tau^69 += einsum(
        "jiba->ijab", tau^68
    )

    del tau^68

    r^2 -= einsum(
        "ijab->abij", tau^69
    )

    r^2 += einsum(
        "ijba->abij", tau^69
    ) / 9

    del tau^69

    tau^72 = zeros((no, no, nv, nv))

    tau^72 += einsum(
        "bckj,kiac->ijab", t^2, tau^67
    )

    del tau^67

    tau^73 += einsum(
        "jiba->ijab", tau^72
    )

    del tau^72

    r^2 += einsum(
        "ijab->abij", tau^73
    ) / 9

    r^2 -= einsum(
        "ijba->abij", tau^73
    )

    del tau^73

    tau^113 = zeros((nv, nv, nv, nv))

    tau^113 += einsum(
        "eabf,cefd->abcd", tau^4, tau^65
    )

    tau^121 = zeros((nv, nv, nv, nv))

    tau^121 += 5 * einsum(
        "abcd->abcd", tau^113
    )

    tau^171 = zeros((no, no, nv, nv))

    tau^171 += einsum(
        "cdij,cabd->ijab", t^2, tau^113
    )

    del tau^113

    tau^172 += einsum(
        "ijab->ijab", tau^171
    )

    del tau^171

    tau^153 = zeros((nv, nv, nv, nv))

    tau^153 += einsum(
        "eabf,cefd->abcd", tau^6, tau^65
    )

    tau^154 += 5 * einsum(
        "abcd->abcd", tau^153
    )

    tau^155 = zeros((no, no, nv, nv))

    tau^155 += einsum(
        "dcij,cabd->ijab", t^2, tau^154
    )

    del tau^154

    tau^167 = zeros((no, no, nv, nv))

    tau^167 += einsum(
        "ijab->ijab", tau^155
    )

    del tau^155

    tau^174 = zeros((no, no, nv, nv))

    tau^174 += einsum(
        "cdij,cabd->ijab", t^2, tau^153
    )

    del tau^153

    tau^175 += einsum(
        "ijab->ijab", tau^174
    )

    del tau^174

    r^2 += 23 * einsum(
        "ijab->abij", tau^175
    ) / 36

    r^2 += 5 * einsum(
        "ijba->abij", tau^175
    ) / 18

    del tau^175

    tau^267 = zeros((no, no, nv, nv))

    tau^267 += einsum(
        "ijcd,acdb->ijab", tau^49, tau^65
    )

    tau^268 = zeros((no, no, nv, nv))

    tau^268 += einsum(
        "acki,kjbc->ijab", t^2, tau^267
    )

    del tau^267

    r^2 += einsum(
        "ijab->abij", tau^268
    ) / 3

    r^2 += 5 * einsum(
        "ijba->abij", tau^268
    ) / 9

    del tau^268

    tau^302 = zeros((no, no, nv, nv))

    tau^302 += einsum(
        "ijcd,acbd->ijab", tau^49, tau^65
    )

    tau^312 += einsum(
        "jiba->ijab", tau^302
    )

    tau^353 += 18 * einsum(
        "jiba->ijab", tau^302
    )

    del tau^302

    tau^313 = zeros((nv, nv, nv, nv))

    tau^313 += einsum(
        "eabf,cedf->abcd", tau^6, tau^65
    )

    tau^320 = zeros((nv, nv, nv, nv))

    tau^320 += 9 * einsum(
        "abcd->abcd", tau^313
    )

    tau^321 += 18 * einsum(
        "abcd->abcd", tau^313
    )

    del tau^313

    tau^365 += 18 * einsum(
        "ijcd,bcda->ijab", tau^0, tau^65
    )

    tau^388 -= 36 * einsum(
        "ijcd,bcda->ijab", tau^48, tau^65
    )

    tau^75 = zeros((no, no, nv, nv))

    tau^75 += einsum(
        "acki,jcbk->ijab", \lambda^2, u
    )

    tau^81 -= 2 * einsum(
        "ijab->ijab", tau^75
    )

    tau^257 += einsum(
        "ijab->ijab", tau^75
    )

    del tau^75

    tau^78 = zeros((no, no, no, no))

    tau^78 += einsum(
        "abij,lkba->ijkl", t^2, u
    )

    tau^79 = zeros((no, no, no, no))

    tau^79 += einsum(
        "lkji->ijkl", tau^78
    )

    tau^310 = zeros((no, no, no, no))

    tau^310 += einsum(
        "klij->ijkl", tau^78
    )

    tau^402 = zeros((no, no, no, no))

    tau^402 += 2 * einsum(
        "ijkl->ijkl", tau^78
    )

    tau^402 += einsum(
        "ijlk->ijkl", tau^78
    )

    tau^407 = zeros((no, no, no, no))

    tau^407 += 7 * einsum(
        "klji->ijkl", tau^78
    )

    tau^407 -= einsum(
        "lkji->ijkl", tau^78
    )

    tau^415 = zeros((no, no, no, no))

    tau^415 += einsum(
        "klji->ijkl", tau^78
    )

    tau^415 += 2 * einsum(
        "lkji->ijkl", tau^78
    )

    tau^79 += einsum(
        "jilk->ijkl", u
    )

    tau^80 = zeros((no, no, nv, nv))

    tau^80 += einsum(
        "abkl,ijkl->ijab", \lambda^2, tau^79
    )

    tau^81 += 5 * einsum(
        "jiba->ijab", tau^80
    )

    del tau^80

    tau^82 = zeros((no, no, nv, nv))

    tau^82 += einsum(
        "bckj,ikca->ijab", t^2, tau^81
    )

    del tau^81

    tau^83 += einsum(
        "ijab->ijab", tau^82
    )

    del tau^82

    tau^84 = zeros((no, no, nv, nv))

    tau^84 += einsum(
        "bckj,kica->ijab", t^2, tau^83
    )

    del tau^83

    r^2 += einsum(
        "jiba->abij", tau^84
    ) / 18

    r^2 += einsum(
        "ijba->abij", tau^84
    ) / 9

    del tau^84

    tau^189 = zeros((no, no, nv, nv))

    tau^189 += einsum(
        "ablk,ijkl->ijab", \lambda^2, tau^79
    )

    tau^190 += einsum(
        "jiab->ijab", tau^189
    )

    tau^191 = zeros((no, no, nv, nv))

    tau^191 += einsum(
        "bckj,ikac->ijab", t^2, tau^190
    )

    del tau^190

    tau^192 = zeros((no, no, nv, nv))

    tau^192 += einsum(
        "bckj,kica->ijab", t^2, tau^191
    )

    del tau^191

    r^2 += 23 * einsum(
        "jiba->abij", tau^192
    ) / 36

    r^2 += 5 * einsum(
        "ijba->abij", tau^192
    ) / 18

    del tau^192

    tau^303 += einsum(
        "jiab->ijab", tau^189
    )

    del tau^189

    tau^312 -= einsum(
        "kibc,jkac->ijab", tau^29, tau^303
    )

    del tau^29

    tau^345 = zeros((no, no, nv, nv))

    tau^345 += 9 * einsum(
        "bckj,kica->ijab", t^2, tau^303
    )

    del tau^303

    tau^312 -= einsum(
        "lkab,kjil->ijab", tau^311, tau^79
    )

    del tau^311

    tau^337 = zeros((no, no, nv, nv))

    tau^337 += 9 * einsum(
        "lkab,kilj->ijab", tau^50, tau^79
    )

    tau^345 += 9 * einsum(
        "lkab,kilj->ijab", tau^27, tau^79
    )

    tau^382 += 10 * einsum(
        "injm,mkln->ijkl", tau^380, tau^79
    )

    del tau^380

    tau^382 -= 36 * einsum(
        "niml,mknj->ijkl", tau^2, tau^79
    )

    tau^382 -= 36 * einsum(
        "im,mklj->ijkl", tau^14, tau^79
    )

    tau^399 = zeros((no, no, no, no))

    tau^399 += 10 * einsum(
        "injm,mkln->ijkl", tau^396, tau^79
    )

    tau^399 += 2 * einsum(
        "niml,mknj->ijkl", tau^396, tau^79
    )

    del tau^396

    tau^414 += einsum(
        "lmkj,kilm->ij", tau^2, tau^79
    )

    r^2 += einsum(
        "lkab,klji->abij", tau^403, tau^79
    )

    del tau^403

    del tau^79

    tau^85 = zeros((no, no, nv, nv))

    tau^85 += einsum(
        "acik,bckj->ijab", \lambda^2, t^2
    )

    tau^86 = zeros((no, no, nv, nv))

    tau^86 += einsum(
        "acki,kjcb->ijab", t^2, tau^85
    )

    tau^87 = zeros((no, no, nv, nv))

    tau^87 += einsum(
        "klab,lkji->ijab", tau^86, u
    )

    tau^96 = zeros((no, no, nv, nv))

    tau^96 += 3 * einsum(
        "ijab->ijab", tau^87
    )

    del tau^87

    tau^157 = zeros((no, no, nv, nv))

    tau^157 += einsum(
        "kiac,jkcb->ijab", tau^86, u
    )

    tau^160 += 3 * einsum(
        "ijba->ijab", tau^157
    )

    del tau^157

    tau^299 = zeros((no, no, nv, nv))

    tau^299 += einsum(
        "kiac,kbjc->ijab", tau^86, u
    )

    r^2 -= 2 * einsum(
        "jiab->abij", tau^299
    ) / 3

    r^2 -= einsum(
        "jiba->abij", tau^299
    ) / 3

    del tau^299

    tau^401 += einsum(
        "ijab->ijab", tau^86
    )

    del tau^86

    r^2 += einsum(
        "klab,ijkl->abij", tau^401, tau^402
    ) / 3

    del tau^401

    del tau^402

    tau^88 = zeros((no, no, nv, nv))

    tau^88 += einsum(
        "klab,lijk->ijab", tau^85, u
    )

    tau^94 = zeros((no, no, nv, nv))

    tau^94 += 3 * einsum(
        "ijab->ijab", tau^88
    )

    del tau^88

    tau^169 = zeros((no, no, nv, nv))

    tau^169 += einsum(
        "cbik,kjca->ijab", t^2, tau^85
    )

    tau^170 = zeros((no, no, nv, nv))

    tau^170 += einsum(
        "ijdc,abdc->ijab", tau^169, u
    )

    tau^172 += einsum(
        "ijab->ijab", tau^170
    )

    del tau^170

    r^2 += 5 * einsum(
        "ijab->abij", tau^172
    ) / 18

    r^2 += 23 * einsum(
        "ijba->abij", tau^172
    ) / 36

    del tau^172

    tau^366 = zeros((no, no, no, no))

    tau^366 += einsum(
        "ijba,klba->ijkl", tau^169, u
    )

    del tau^169

    tau^382 += 24 * einsum(
        "jlik->ijkl", tau^366
    )

    tau^382 += 12 * einsum(
        "jlki->ijkl", tau^366
    )

    del tau^366

    tau^271 = zeros((no, no, nv, nv))

    tau^271 += einsum(
        "acdb,ijcd->ijab", tau^65, tau^85
    )

    tau^272 = zeros((no, no, nv, nv))

    tau^272 += einsum(
        "acki,kjbc->ijab", t^2, tau^271
    )

    del tau^271

    tau^275 = zeros((no, no, nv, nv))

    tau^275 += einsum(
        "ijab->ijab", tau^272
    )

    del tau^272

    tau^347 = zeros((no, no, nv, nv))

    tau^347 += einsum(
        "acik,kjcb->ijab", t^2, tau^85
    )

    tau^349 += einsum(
        "ijab->ijab", tau^347
    )

    tau^353 += 18 * einsum(
        "kibc,kjac->ijab", tau^349, u
    )

    r^2 += einsum(
        "kjac,kbic->abij", tau^349, u
    )

    del tau^349

    tau^356 += 18 * einsum(
        "ijab->ijab", tau^347
    )

    tau^378 += einsum(
        "ijab->ijab", tau^347
    )

    tau^382 -= 36 * einsum(
        "jlab,kiab->ijkl", tau^378, u
    )

    del tau^378

    tau^89 = zeros((nv, nv))

    tau^89 += einsum(
        "caij,cbji->ab", \lambda^2, t^2
    )

    tau^90 = zeros((no, no, nv, nv))

    tau^90 += einsum(
        "ac,ibcj->ijab", tau^89, u
    )

    tau^94 -= einsum(
        "ijab->ijab", tau^90
    )

    del tau^90

    tau^91 = zeros((no, no, nv, nv))

    tau^91 += einsum(
        "ac,ijbc->ijab", tau^89, u
    )

    tau^92 += einsum(
        "ijab->ijab", tau^91
    )

    tau^93 = zeros((no, no, nv, nv))

    tau^93 += einsum(
        "bckj,kiac->ijab", t^2, tau^92
    )

    del tau^92

    tau^94 += einsum(
        "ijab->ijab", tau^93
    )

    del tau^93

    tau^95 = zeros((no, no, nv, nv))

    tau^95 += einsum(
        "bckj,kica->ijab", t^2, tau^94
    )

    del tau^94

    tau^96 += einsum(
        "jiba->ijab", tau^95
    )

    del tau^95

    r^2 += 2 * einsum(
        "ijab->abij", tau^96
    ) / 9

    r^2 += einsum(
        "jiab->abij", tau^96
    ) / 9

    del tau^96

    tau^257 += 2 * einsum(
        "ijab->ijab", tau^91
    )

    tau^257 -= einsum(
        "jiab->ijab", tau^91
    )

    del tau^91

    tau^260 = zeros((no, no, nv, nv))

    tau^260 += einsum(
        "cb,acij->ijab", tau^89, t^2
    )

    tau^261 += einsum(
        "ijab->ijab", tau^260
    )

    del tau^260

    tau^262 = zeros((nv, nv))

    tau^262 += einsum(
        "ijac,ijcb->ab", tau^261, u
    )

    del tau^261

    tau^263 = zeros((no, no, nv, nv))

    tau^263 += einsum(
        "ac,bcij->ijab", tau^262, t^2
    )

    del tau^262

    tau^264 = zeros((no, no, nv, nv))

    tau^264 += einsum(
        "ijba->ijab", tau^263
    )

    del tau^263

    tau^97 = zeros((no, no, nv, nv))

    tau^97 += einsum(
        "caik,cbjk->ijab", \lambda^2, t^2
    )

    tau^98 = zeros((no, no, nv, nv))

    tau^98 += einsum(
        "ijcd,acdb->ijab", tau^97, u
    )

    tau^99 = zeros((no, no, nv, nv))

    tau^99 += einsum(
        "ackj,kibc->ijab", t^2, tau^98
    )

    del tau^98

    r^2 += 5 * einsum(
        "ijab->abij", tau^99
    ) / 9

    r^2 += einsum(
        "ijba->abij", tau^99
    ) / 3

    del tau^99

    tau^111 = zeros((no, no, nv, nv))

    tau^111 += einsum(
        "ackj,kicb->ijab", t^2, tau^97
    )

    tau^112 = zeros((no, no, nv, nv))

    tau^112 += einsum(
        "ijdc,abdc->ijab", tau^111, u
    )

    tau^148 += 5 * einsum(
        "ijab->ijab", tau^112
    )

    del tau^112

    tau^367 = zeros((no, no, no, no))

    tau^367 += einsum(
        "ijba,lkab->ijkl", tau^111, u
    )

    del tau^111

    tau^382 += 12 * einsum(
        "jlik->ijkl", tau^367
    )

    tau^382 += 24 * einsum(
        "jlki->ijkl", tau^367
    )

    del tau^367

    tau^100 = zeros((no, no, nv, nv))

    tau^100 += einsum(
        "caki,bckj->ijab", \lambda^2, t^2
    )

    tau^101 = zeros((no, no, nv, nv))

    tau^101 += einsum(
        "ijcd,acdb->ijab", tau^100, u
    )

    tau^102 = zeros((no, no, nv, nv))

    tau^102 += einsum(
        "acki,kjbc->ijab", t^2, tau^101
    )

    r^2 += 23 * einsum(
        "ijab->abij", tau^102
    ) / 36

    r^2 += einsum(
        "ijba->abij", tau^102
    ) / 3

    del tau^102

    tau^312 += einsum(
        "jiba->ijab", tau^101
    )

    del tau^101

    tau^127 = zeros((no, no, nv, nv))

    tau^127 += einsum(
        "jkcb,ikca->ijab", tau^10, tau^100
    )

    tau^136 += 3 * einsum(
        "jiba->ijab", tau^127
    )

    del tau^127

    tau^182 = zeros((no, no, nv, nv))

    tau^182 += einsum(
        "ijab->ijab", tau^100
    )

    tau^187 = zeros((no, no, nv, nv))

    tau^187 += einsum(
        "klab,lijk->ijab", tau^100, u
    )

    tau^188 = zeros((no, no, nv, nv))

    tau^188 += einsum(
        "acki,kjcb->ijab", t^2, tau^187
    )

    del tau^187

    r^2 += 5 * einsum(
        "ijba->abij", tau^188
    ) / 18

    r^2 += 23 * einsum(
        "jiba->abij", tau^188
    ) / 36

    del tau^188

    tau^292 = zeros((no, no, nv, nv))

    tau^292 += einsum(
        "ikca,kcbj->ijab", tau^100, u
    )

    tau^293 += einsum(
        "ijab->ijab", tau^292
    )

    del tau^292

    tau^294 = zeros((no, no, nv, nv))

    tau^294 += einsum(
        "bckj,kiac->ijab", t^2, tau^293
    )

    del tau^293

    r^2 -= einsum(
        "jiba->abij", tau^294
    ) / 3

    r^2 -= 2 * einsum(
        "jiab->abij", tau^294
    ) / 3

    r^2 -= 2 * einsum(
        "ijba->abij", tau^294
    ) / 3

    r^2 -= einsum(
        "ijab->abij", tau^294
    ) / 3

    del tau^294

    tau^296 = zeros((no, no, nv, nv))

    tau^296 += einsum(
        "ikca,kcjb->ijab", tau^100, u
    )

    tau^297 += einsum(
        "ijab->ijab", tau^296
    )

    del tau^296

    tau^298 = zeros((no, no, nv, nv))

    tau^298 += einsum(
        "bckj,kiac->ijab", t^2, tau^297
    )

    del tau^297

    r^2 -= 2 * einsum(
        "jiba->abij", tau^298
    ) / 3

    r^2 -= einsum(
        "jiab->abij", tau^298
    ) / 3

    r^2 -= einsum(
        "ijba->abij", tau^298
    ) / 3

    r^2 -= 2 * einsum(
        "ijab->abij", tau^298
    ) / 3

    del tau^298

    tau^357 = zeros((no, no, nv, nv))

    tau^357 += einsum(
        "ijcd,acbd->ijab", tau^100, u
    )

    tau^365 -= einsum(
        "ijba->ijab", tau^357
    )

    tau^400 += einsum(
        "ijab->ijab", tau^357
    )

    del tau^357

    tau^382 -= 12 * einsum(
        "ilab,kabj->ijkl", tau^100, u
    )

    tau^105 = zeros((no, no, nv, nv))

    tau^105 += einsum(
        "caki,cbjk->ijab", \lambda^2, t^2
    )

    tau^106 = zeros((no, no, nv, nv))

    tau^106 += einsum(
        "ijcd,acdb->ijab", tau^105, u
    )

    tau^107 = zeros((no, no, nv, nv))

    tau^107 += einsum(
        "ackj,kibc->ijab", t^2, tau^106
    )

    del tau^106

    tau^108 += einsum(
        "ijab->ijab", tau^107
    )

    del tau^107

    r^2 += 5 * einsum(
        "ijab->abij", tau^108
    ) / 18

    r^2 += 2 * einsum(
        "ijba->abij", tau^108
    ) / 3

    del tau^108

    tau^114 = zeros((no, no, nv, nv))

    tau^114 += einsum(
        "acik,kjbc->ijab", t^2, u
    )

    tau^116 -= einsum(
        "ijab->ijab", tau^114
    )

    tau^318 -= einsum(
        "jiab->ijab", tau^114
    )

    del tau^114

    tau^116 += einsum(
        "jabi->ijab", u
    )

    tau^117 = zeros((nv, nv, nv, nv))

    tau^117 += einsum(
        "ijcd,ijab->abcd", tau^100, tau^116
    )

    tau^121 -= 6 * einsum(
        "cdab->abcd", tau^117
    )

    tau^151 -= 6 * einsum(
        "cdab->abcd", tau^117
    )

    del tau^117

    tau^230 = zeros((no, no, nv, nv))

    tau^230 += einsum(
        "klab,iklj->ijab", tau^116, tau^2
    )

    tau^231 = zeros((no, no, nv, nv))

    tau^231 += einsum(
        "bckj,kiac->ijab", t^2, tau^230
    )

    tau^232 = zeros((no, no, nv, nv))

    tau^232 += einsum(
        "jiba->ijab", tau^231
    )

    del tau^231

    tau^312 -= einsum(
        "jiba->ijab", tau^230
    )

    del tau^230

    tau^236 = zeros((no, no, nv, nv))

    tau^236 += einsum(
        "kiac,kjbc->ijab", tau^116, tau^49
    )

    tau^238 = zeros((no, no, nv, nv))

    tau^238 += einsum(
        "jiba->ijab", tau^236
    )

    del tau^236

    tau^240 = zeros((no, no, nv, nv))

    tau^240 += einsum(
        "klab,ikjl->ijab", tau^116, tau^2
    )

    tau^241 = zeros((no, no, nv, nv))

    tau^241 += einsum(
        "bckj,kiac->ijab", t^2, tau^240
    )

    del tau^240

    tau^242 += einsum(
        "ijba->ijab", tau^241
    )

    del tau^241

    tau^244 = zeros((no, no, nv, nv))

    tau^244 += einsum(
        "kiac,kjbc->ijab", tau^116, tau^85
    )

    tau^247 = zeros((no, no, nv, nv))

    tau^247 += einsum(
        "jiba->ijab", tau^244
    )

    del tau^244

    tau^246 = zeros((no, no, nv, nv))

    tau^246 += einsum(
        "klab,kilj->ijab", tau^116, tau^2
    )

    tau^247 += einsum(
        "jiba->ijab", tau^246
    )

    del tau^246

    tau^251 = zeros((no, no, nv, nv))

    tau^251 += einsum(
        "klab,kijl->ijab", tau^116, tau^2
    )

    tau^252 = zeros((no, no, nv, nv))

    tau^252 += einsum(
        "jiba->ijab", tau^251
    )

    tau^345 -= 9 * einsum(
        "ijba->ijab", tau^251
    )

    del tau^251

    tau^312 -= einsum(
        "kjbc,kiac->ijab", tau^116, tau^306
    )

    tau^337 -= 9 * einsum(
        "kibc,kjac->ijab", tau^116, tau^48
    )

    tau^353 -= 18 * einsum(
        "kiac,kjbc->ijab", tau^0, tau^116
    )

    del tau^0

    tau^118 = zeros((no, no, nv, nv))

    tau^118 += 2 * einsum(
        "jiab->ijab", u
    )

    tau^118 -= einsum(
        "jiba->ijab", u
    )

    tau^119 = zeros((no, no, nv, nv))

    tau^119 += einsum(
        "bcjk,kiac->ijab", t^2, tau^118
    )

    tau^120 = zeros((nv, nv, nv, nv))

    tau^120 += einsum(
        "jiba,ijcd->abcd", tau^119, tau^25
    )

    tau^121 -= 6 * einsum(
        "bdac->abcd", tau^120
    )

    tau^122 = zeros((no, no, nv, nv))

    tau^122 += einsum(
        "dcij,cabd->ijab", t^2, tau^121
    )

    del tau^121

    tau^148 += einsum(
        "ijab->ijab", tau^122
    )

    del tau^122

    tau^151 -= 6 * einsum(
        "bdac->abcd", tau^120
    )

    del tau^120

    tau^152 = zeros((no, no, nv, nv))

    tau^152 += einsum(
        "cdij,cabd->ijab", t^2, tau^151
    )

    del tau^151

    tau^167 += einsum(
        "ijab->ijab", tau^152
    )

    del tau^152

    tau^343 += einsum(
        "jiba->ijab", tau^119
    )

    del tau^119

    tau^130 = zeros((no, no, nv, nv))

    tau^130 += einsum(
        "caki,kjbc->ijab", t^2, u
    )

    tau^133 = zeros((no, no, nv, nv))

    tau^133 += einsum(
        "ijab->ijab", tau^130
    )

    tau^369 = zeros((no, no, nv, nv))

    tau^369 -= einsum(
        "ijab->ijab", tau^130
    )

    del tau^130

    tau^131 = zeros((no, no, nv, nv))

    tau^131 += 2 * einsum(
        "abij->ijab", t^2
    )

    tau^131 -= einsum(
        "baij->ijab", t^2
    )

    tau^132 = zeros((no, no, nv, nv))

    tau^132 += einsum(
        "kica,kjcb->ijab", tau^131, u
    )

    tau^133 -= einsum(
        "ijab->ijab", tau^132
    )

    tau^134 = zeros((no, no, nv, nv))

    tau^134 += einsum(
        "ijcd,acbd->ijab", tau^133, tau^6
    )

    tau^136 += 2 * einsum(
        "ijab->ijab", tau^134
    )

    tau^312 += einsum(
        "ijab->ijab", tau^134
    )

    del tau^134

    tau^135 = zeros((no, no, nv, nv))

    tau^135 += einsum(
        "ikca,jkcb->ijab", tau^133, tau^25
    )

    tau^136 += 3 * einsum(
        "ijab->ijab", tau^135
    )

    del tau^135

    tau^137 = zeros((no, no, nv, nv))

    tau^137 += einsum(
        "bckj,ikca->ijab", t^2, tau^136
    )

    del tau^136

    tau^148 += 2 * einsum(
        "jiba->ijab", tau^137
    )

    del tau^137

    tau^158 = zeros((no, no, nv, nv))

    tau^158 += einsum(
        "ijcd,acbd->ijab", tau^133, tau^4
    )

    tau^160 += 2 * einsum(
        "ijab->ijab", tau^158
    )

    del tau^158

    tau^159 = zeros((no, no, nv, nv))

    tau^159 += einsum(
        "jkcb,ikca->ijab", tau^100, tau^133
    )

    tau^160 += 3 * einsum(
        "ijab->ijab", tau^159
    )

    del tau^159

    tau^161 = zeros((no, no, nv, nv))

    tau^161 += einsum(
        "bckj,ikca->ijab", t^2, tau^160
    )

    del tau^160

    tau^167 += 2 * einsum(
        "jiba->ijab", tau^161
    )

    del tau^161

    tau^312 += einsum(
        "ikca,jkcb->ijab", tau^133, tau^27
    )

    tau^353 += 18 * einsum(
        "ikca,jkcb->ijab", tau^133, tau^26
    )

    del tau^133

    tau^369 += einsum(
        "ijab->ijab", tau^132
    )

    del tau^132

    tau^304 = zeros((no, no, nv, nv))

    tau^304 += einsum(
        "bcjk,kica->ijab", \lambda^2, tau^131
    )

    tau^305 = zeros((no, no, nv, nv))

    tau^305 += einsum(
        "bcjk,ikac->ijab", t^2, tau^304
    )

    tau^308 -= einsum(
        "jiba->ijab", tau^305
    )

    del tau^305

    tau^312 -= einsum(
        "kjac,kibc->ijab", tau^118, tau^308
    )

    del tau^118

    tau^365 += 18 * einsum(
        "kjbc,kica->ijab", tau^308, u
    )

    r^2 += einsum(
        "kjbc,kaci->abij", tau^308, u
    )

    del tau^308

    tau^312 -= einsum(
        "ijdc,bcda->ijab", tau^304, tau^65
    )

    tau^411 -= 9 * einsum(
        "ikab,kjba->ij", tau^131, tau^404
    )

    del tau^131

    tau^138 = zeros((no, no, nv, nv))

    tau^138 += einsum(
        "caik,kjbc->ijab", t^2, u
    )

    tau^139 = zeros((no, no, nv, nv))

    tau^139 += einsum(
        "jkcb,ikca->ijab", tau^138, tau^25
    )

    tau^146 = zeros((no, no, nv, nv))

    tau^146 += einsum(
        "ijab->ijab", tau^139
    )

    del tau^139

    tau^162 = zeros((no, no, nv, nv))

    tau^162 += einsum(
        "ikca,jkcb->ijab", tau^100, tau^138
    )

    tau^165 += einsum(
        "ijab->ijab", tau^162
    )

    del tau^162

    tau^193 = zeros((no, no, nv, nv))

    tau^193 += einsum(
        "ijcd,acbd->ijab", tau^138, tau^4
    )

    tau^194 = zeros((no, no, nv, nv))

    tau^194 += einsum(
        "ackj,ikcb->ijab", t^2, tau^193
    )

    tau^197 += einsum(
        "ijab->ijab", tau^194
    )

    del tau^194

    r^2 -= einsum(
        "ijab->abij", tau^197
    )

    r^2 += 2 * einsum(
        "ijba->abij", tau^197
    ) / 9

    del tau^197

    tau^345 += 9 * einsum(
        "jiab->ijab", tau^193
    )

    del tau^193

    tau^345 -= 9 * einsum(
        "jkca,kibc->ijab", tau^138, tau^30
    )

    tau^382 -= 36 * einsum(
        "jkab,ilab->ijkl", tau^138, tau^306
    )

    del tau^306

    tau^388 += 4 * einsum(
        "jicd,acbd->ijab", tau^138, tau^6
    )

    tau^388 -= 36 * einsum(
        "jkca,ikcb->ijab", tau^138, tau^26
    )

    del tau^138

    tau^140 = zeros((no, no, nv, nv))

    tau^140 += einsum(
        "acik,cbjk->ijab", \lambda^2, t^2
    )

    tau^141 = zeros((no, no, nv, nv))

    tau^141 += einsum(
        "ackj,kicb->ijab", t^2, tau^140
    )

    tau^142 = zeros((no, no, nv, nv))

    tau^142 += einsum(
        "ikac,jkcb->ijab", tau^141, u
    )

    tau^146 += einsum(
        "jiab->ijab", tau^142
    )

    del tau^142

    tau^149 = zeros((no, no, nv, nv))

    tau^149 += einsum(
        "ikac,kbjc->ijab", tau^141, u
    )

    del tau^141

    tau^167 -= 6 * einsum(
        "ijab->ijab", tau^149
    )

    del tau^149

    tau^225 = zeros((no, no, nv, nv))

    tau^225 += einsum(
        "kiac,kjbc->ijab", tau^116, tau^140
    )

    tau^228 = zeros((no, no, nv, nv))

    tau^228 += einsum(
        "jiba->ijab", tau^225
    )

    del tau^225

    tau^265 = zeros((no, no, nv, nv))

    tau^265 += einsum(
        "ijcd,acdb->ijab", tau^140, tau^65
    )

    tau^266 = zeros((no, no, nv, nv))

    tau^266 += einsum(
        "ackj,kibc->ijab", t^2, tau^265
    )

    del tau^265

    r^2 += einsum(
        "ijab->abij", tau^266
    ) / 3

    r^2 += 23 * einsum(
        "ijba->abij", tau^266
    ) / 36

    del tau^266

    tau^330 = zeros((no, no, nv, nv))

    tau^330 += einsum(
        "acik,kjcb->ijab", t^2, tau^140
    )

    tau^332 += einsum(
        "ijab->ijab", tau^330
    )

    del tau^330

    tau^337 += 9 * einsum(
        "kjbc,kiac->ijab", tau^332, u
    )

    r^2 += einsum(
        "kibc,kajc->abij", tau^332, u
    )

    del tau^332

    tau^143 = zeros((no, no, nv, nv))

    tau^143 += einsum(
        "caik,kjcb->ijab", t^2, u
    )

    tau^144 += einsum(
        "ijab->ijab", tau^143
    )

    del tau^143

    tau^145 = zeros((no, no, nv, nv))

    tau^145 += einsum(
        "jkcb,ikca->ijab", tau^100, tau^144
    )

    del tau^100

    tau^146 += einsum(
        "jiba->ijab", tau^145
    )

    del tau^145

    tau^147 = zeros((no, no, nv, nv))

    tau^147 += einsum(
        "bckj,kiac->ijab", t^2, tau^146
    )

    del tau^146

    tau^148 += 6 * einsum(
        "ijba->ijab", tau^147
    )

    del tau^147

    r^2 += einsum(
        "ijab->abij", tau^148
    ) / 9

    r^2 += einsum(
        "ijba->abij", tau^148
    ) / 18

    del tau^148

    tau^164 = zeros((no, no, nv, nv))

    tau^164 += einsum(
        "ikca,jkcb->ijab", tau^144, tau^25
    )

    tau^165 += einsum(
        "jiba->ijab", tau^164
    )

    del tau^164

    tau^166 = zeros((no, no, nv, nv))

    tau^166 += einsum(
        "bckj,kiac->ijab", t^2, tau^165
    )

    del tau^165

    tau^167 += 6 * einsum(
        "ijba->ijab", tau^166
    )

    del tau^166

    r^2 += einsum(
        "ijab->abij", tau^167
    ) / 18

    r^2 += einsum(
        "ijba->abij", tau^167
    ) / 9

    del tau^167

    tau^223 = zeros((no, no, nv, nv))

    tau^223 += einsum(
        "ijcd,acbd->ijab", tau^144, tau^4
    )

    del tau^4

    tau^224 = zeros((no, no, nv, nv))

    tau^224 += einsum(
        "bckj,ikca->ijab", t^2, tau^223
    )

    del tau^223

    r^2 += 11 * einsum(
        "ijba->abij", tau^224
    ) / 18

    r^2 += 2 * einsum(
        "ijab->abij", tau^224
    ) / 9

    del tau^224

    tau^233 = zeros((no, no, nv, nv))

    tau^233 += einsum(
        "ijcd,acbd->ijab", tau^144, tau^6
    )

    del tau^6

    tau^234 = zeros((no, no, nv, nv))

    tau^234 += einsum(
        "bckj,ikca->ijab", t^2, tau^233
    )

    r^2 += 2 * einsum(
        "ijba->abij", tau^234
    ) / 9

    r^2 += 11 * einsum(
        "ijab->abij", tau^234
    ) / 18

    del tau^234

    tau^345 += 9 * einsum(
        "jiab->ijab", tau^233
    )

    del tau^233

    tau^337 += 9 * einsum(
        "jkca,ikcb->ijab", tau^144, tau^26
    )

    tau^345 += 9 * einsum(
        "jkca,ikcb->ijab", tau^144, tau^27
    )

    tau^181 = zeros((no, no, nv, nv))

    tau^181 += einsum(
        "caik,bcjk->ijab", \lambda^2, t^2
    )

    tau^182 += einsum(
        "ijab->ijab", tau^181
    )

    del tau^181

    tau^183 = zeros((no, no, nv, nv))

    tau^183 += einsum(
        "klab,likj->ijab", tau^182, u
    )

    del tau^182

    tau^184 += einsum(
        "ijab->ijab", tau^183
    )

    del tau^183

    tau^185 = zeros((no, no, nv, nv))

    tau^185 += einsum(
        "bckj,kica->ijab", t^2, tau^184
    )

    del tau^184

    tau^186 += einsum(
        "jiba->ijab", tau^185
    )

    del tau^185

    r^2 += einsum(
        "ijba->abij", tau^186
    ) / 9

    r^2 += einsum(
        "jiba->abij", tau^186
    ) / 18

    del tau^186

    tau^217 -= einsum(
        "jaib->ijab", u
    )

    tau^218 = zeros((no, no, nv, nv))

    tau^218 += einsum(
        "kiac,kjbc->ijab", tau^217, tau^48
    )

    tau^219 = zeros((no, no, nv, nv))

    tau^219 -= einsum(
        "bckj,kiac->ijab", t^2, tau^218
    )

    del tau^218

    tau^222 = zeros((no, no, nv, nv))

    tau^222 += einsum(
        "ijba->ijab", tau^219
    )

    del tau^219

    tau^220 = zeros((no, no, nv, nv))

    tau^220 += einsum(
        "kilj,klab->ijab", tau^2, tau^217
    )

    tau^221 = zeros((no, no, nv, nv))

    tau^221 -= einsum(
        "bckj,kiac->ijab", t^2, tau^220
    )

    tau^222 += einsum(
        "jiba->ijab", tau^221
    )

    del tau^221

    r^2 -= 2 * einsum(
        "ijab->abij", tau^222
    ) / 9

    r^2 += einsum(
        "ijba->abij", tau^222
    )

    del tau^222

    tau^312 += einsum(
        "jiba->ijab", tau^220
    )

    del tau^220

    tau^237 = zeros((no, no, nv, nv))

    tau^237 += einsum(
        "kiac,kjbc->ijab", tau^217, tau^85
    )

    del tau^85

    tau^238 -= einsum(
        "jiba->ijab", tau^237
    )

    del tau^237

    tau^239 = zeros((no, no, nv, nv))

    tau^239 += einsum(
        "bckj,ikca->ijab", t^2, tau^238
    )

    del tau^238

    tau^242 += einsum(
        "jiba->ijab", tau^239
    )

    del tau^239

    r^2 -= 4 * einsum(
        "ijab->abij", tau^242
    ) / 9

    r^2 -= einsum(
        "ijba->abij", tau^242
    ) / 3

    del tau^242

    tau^245 = zeros((no, no, nv, nv))

    tau^245 += einsum(
        "kiac,kjbc->ijab", tau^217, tau^49
    )

    del tau^49

    tau^247 -= einsum(
        "jiba->ijab", tau^245
    )

    del tau^245

    tau^248 = zeros((no, no, nv, nv))

    tau^248 += einsum(
        "bckj,ikca->ijab", t^2, tau^247
    )

    del tau^247

    tau^254 += einsum(
        "jiba->ijab", tau^248
    )

    del tau^248

    tau^250 = zeros((no, no, nv, nv))

    tau^250 += einsum(
        "kjbc,kiac->ijab", tau^140, tau^217
    )

    del tau^140

    tau^252 -= einsum(
        "jiba->ijab", tau^250
    )

    tau^345 += 9 * einsum(
        "ijba->ijab", tau^250
    )

    del tau^250

    tau^312 -= einsum(
        "kjbc,ikca->ijab", tau^217, tau^304
    )

    del tau^304

    tau^316 = zeros((nv, nv, nv, nv))

    tau^316 += einsum(
        "ijcd,ijab->abcd", tau^217, tau^27
    )

    del tau^27

    tau^320 += 9 * einsum(
        "abcd->abcd", tau^316
    )

    tau^321 += 18 * einsum(
        "abcd->abcd", tau^316
    )

    del tau^316

    tau^337 += einsum(
        "klba,ikjl->ijab", tau^217, tau^334
    )

    del tau^334

    tau^345 += 9 * einsum(
        "ikjl,klba->ijab", tau^2, tau^217
    )

    tau^365 -= 2 * einsum(
        "iklj,klba->ijab", tau^2, tau^217
    )

    tau^399 -= 36 * einsum(
        "lkab,ijab->ijkl", tau^217, tau^26
    )

    tau^226 = zeros((no, no, nv, nv))

    tau^226 += einsum(
        "acki,cbjk->ijab", \lambda^2, t^2
    )

    tau^227 = zeros((no, no, nv, nv))

    tau^227 += einsum(
        "kiac,kjbc->ijab", tau^217, tau^226
    )

    tau^228 -= einsum(
        "jiba->ijab", tau^227
    )

    del tau^227

    tau^229 = zeros((no, no, nv, nv))

    tau^229 += einsum(
        "bckj,ikca->ijab", t^2, tau^228
    )

    del tau^228

    tau^232 += einsum(
        "ijba->ijab", tau^229
    )

    del tau^229

    r^2 -= 11 * einsum(
        "ijab->abij", tau^232
    ) / 18

    r^2 -= einsum(
        "ijba->abij", tau^232
    ) / 3

    del tau^232

    tau^249 = zeros((no, no, nv, nv))

    tau^249 += einsum(
        "kiac,kjbc->ijab", tau^116, tau^226
    )

    tau^252 += einsum(
        "jiba->ijab", tau^249
    )

    tau^253 = zeros((no, no, nv, nv))

    tau^253 += einsum(
        "bckj,ikca->ijab", t^2, tau^252
    )

    del tau^252

    tau^254 += einsum(
        "ijba->ijab", tau^253
    )

    del tau^253

    r^2 -= 2 * einsum(
        "ijab->abij", tau^254
    ) / 9

    r^2 -= 2 * einsum(
        "ijba->abij", tau^254
    ) / 3

    del tau^254

    tau^345 -= 9 * einsum(
        "ijba->ijab", tau^249
    )

    del tau^249

    tau^273 = zeros((no, no, nv, nv))

    tau^273 += einsum(
        "ijcd,acdb->ijab", tau^226, tau^65
    )

    tau^274 = zeros((no, no, nv, nv))

    tau^274 += einsum(
        "ackj,kibc->ijab", t^2, tau^273
    )

    del tau^273

    tau^275 += einsum(
        "ijab->ijab", tau^274
    )

    del tau^274

    r^2 += 2 * einsum(
        "ijab->abij", tau^275
    ) / 3

    r^2 += 5 * einsum(
        "ijba->abij", tau^275
    ) / 18

    del tau^275

    tau^328 = zeros((no, no, nv, nv))

    tau^328 += 9 * einsum(
        "ijab->ijab", tau^226
    )

    tau^340 += einsum(
        "ijab->ijab", tau^226
    )

    del tau^226

    tau^342 = zeros((no, no, nv, nv))

    tau^342 += 9 * einsum(
        "ackj,kicb->ijab", t^2, tau^340
    )

    del tau^340

    tau^255 = zeros((no, no, nv, nv))

    tau^255 += einsum(
        "acik,jckb->ijab", \lambda^2, u
    )

    tau^257 += einsum(
        "ijab->ijab", tau^255
    )

    tau^258 = zeros((no, no, nv, nv))

    tau^258 += einsum(
        "bcjk,kiac->ijab", t^2, tau^257
    )

    del tau^257

    tau^259 = zeros((no, no, nv, nv))

    tau^259 += einsum(
        "bckj,kica->ijab", t^2, tau^258
    )

    del tau^258

    tau^264 -= 2 * einsum(
        "ijab->ijab", tau^259
    )

    del tau^259

    r^2 += einsum(
        "ijba->abij", tau^264
    ) / 18

    r^2 += einsum(
        "jiba->abij", tau^264
    ) / 9

    del tau^264

    tau^279 += einsum(
        "ijab->ijab", tau^255
    )

    tau^280 = zeros((no, no, nv, nv))

    tau^280 += einsum(
        "bckj,kiac->ijab", t^2, tau^279
    )

    del tau^279

    tau^281 = zeros((no, no, nv, nv))

    tau^281 += einsum(
        "bckj,kica->ijab", t^2, tau^280
    )

    del tau^280

    r^2 -= 2 * einsum(
        "ijab->abij", tau^281
    ) / 9

    r^2 -= 11 * einsum(
        "ijba->abij", tau^281
    ) / 18

    r^2 -= 4 * einsum(
        "jiab->abij", tau^281
    ) / 9

    r^2 -= 2 * einsum(
        "jiba->abij", tau^281
    ) / 9

    del tau^281

    tau^405 -= einsum(
        "ijab->ijab", tau^255
    )

    tau^411 += 9 * einsum(
        "baik,kjab->ij", t^2, tau^405
    )

    del tau^405

    tau^414 -= einsum(
        "bakj,kiba->ij", t^2, tau^255
    )

    del tau^255

    tau^309 = zeros((no, no, nv, nv))

    tau^309 += einsum(
        "caki,cbkj->ijab", \lambda^2, t^2
    )

    tau^312 -= einsum(
        "jicd,cbad->ijab", tau^309, tau^56
    )

    del tau^56

    tau^353 += 18 * einsum(
        "jicd,bcad->ijab", tau^309, u
    )

    tau^365 += 18 * einsum(
        "ijcd,bcda->ijab", tau^309, u
    )

    tau^382 += 36 * einsum(
        "ilab,kajb->ijkl", tau^309, u
    )

    tau^395 = zeros((no, no, nv, nv))

    tau^395 -= 3 * einsum(
        "ijab->ijab", tau^309
    )

    del tau^309

    tau^310 += einsum(
        "jilk->ijkl", u
    )

    tau^312 += einsum(
        "kjli,lkab->ijab", tau^310, tau^50
    )

    del tau^50

    r^2 -= einsum(
        "acik,jkcb->abij", t^2, tau^312
    )

    del tau^312

    tau^345 -= 9 * einsum(
        "lkba,lijk->ijab", tau^30, tau^310
    )

    del tau^30

    tau^382 += einsum(
        "mkjn,niml->ijkl", tau^310, tau^381
    )

    del tau^381

    tau^399 -= 4 * einsum(
        "mknl,injm->ijkl", tau^310, tau^397
    )

    del tau^397

    tau^399 += einsum(
        "mkjn,niml->ijkl", tau^310, tau^398
    )

    del tau^398

    tau^399 -= 36 * einsum(
        "im,mkjl->ijkl", tau^14, tau^310
    )

    r^2 -= einsum(
        "klij,klab->abij", tau^310, tau^347
    )

    del tau^347

    del tau^310

    tau^314 = zeros((no, no, nv, nv))

    tau^314 += 7 * einsum(
        "abij->ijab", t^2
    )

    tau^314 -= einsum(
        "abji->ijab", t^2
    )

    tau^315 = zeros((nv, nv, nv, nv))

    tau^315 += einsum(
        "abij,ijcd->abcd", \lambda^2, tau^314
    )

    tau^320 += einsum(
        "aebf,ecfd->abcd", tau^315, u
    )

    del tau^315

    tau^351 = zeros((no, no, nv, nv))

    tau^351 += einsum(
        "caik,kjbc->ijab", \lambda^2, tau^314
    )

    del tau^314

    tau^318 += einsum(
        "iabj->ijab", u
    )

    tau^319 = zeros((nv, nv, nv, nv))

    tau^319 += einsum(
        "jicd,ijab->abcd", tau^26, tau^318
    )

    del tau^26

    del tau^318

    tau^320 -= 9 * einsum(
        "bdac->abcd", tau^319
    )

    r^2 -= einsum(
        "cdij,cabd->abij", t^2, tau^320
    ) / 9

    del tau^320

    tau^321 -= 18 * einsum(
        "bdac->abcd", tau^319
    )

    del tau^319

    r^2 -= einsum(
        "dcij,cbad->abij", t^2, tau^321
    ) / 18

    del tau^321

    tau^322 = zeros((no, no, nv, nv))

    tau^322 -= einsum(
        "abij->ijab", \lambda^2
    )

    tau^322 += 7 * einsum(
        "abji->ijab", \lambda^2
    )

    tau^323 = zeros((no, no, nv, nv))

    tau^323 += einsum(
        "cbjk,kica->ijab", t^2, tau^322
    )

    del tau^322

    tau^324 += einsum(
        "ijab->ijab", tau^323
    )

    del tau^323

    tau^325 = zeros((no, no, nv, nv))

    tau^325 += einsum(
        "ijcd,cadb->ijab", tau^324, u
    )

    del tau^324

    tau^337 += einsum(
        "ijba->ijab", tau^325
    )

    tau^345 += einsum(
        "ijba->ijab", tau^325
    )

    del tau^325

    tau^326 = zeros((no, no, nv, nv))

    tau^326 += 7 * einsum(
        "abij->ijab", \lambda^2
    )

    tau^326 -= einsum(
        "abji->ijab", \lambda^2
    )

    tau^327 = zeros((no, no, nv, nv))

    tau^327 += einsum(
        "bcjk,ikac->ijab", t^2, tau^326
    )

    del tau^326

    tau^328 += einsum(
        "ijab->ijab", tau^327
    )

    del tau^327

    tau^329 = zeros((no, no, nv, nv))

    tau^329 += einsum(
        "ijcd,acbd->ijab", tau^328, tau^65
    )

    del tau^328

    tau^337 += einsum(
        "ijba->ijab", tau^329
    )

    tau^345 += einsum(
        "ijba->ijab", tau^329
    )

    del tau^329

    tau^333 = zeros((no, no, nv, nv))

    tau^333 += einsum(
        "acki,bcjk->ijab", \lambda^2, t^2
    )

    tau^337 -= einsum(
        "kibc,kjac->ijab", tau^217, tau^333
    )

    del tau^333

    tau^335 = zeros((no, no, nv, nv))

    tau^335 += einsum(
        "abij->ijab", \lambda^2
    )

    tau^335 += 2 * einsum(
        "abji->ijab", \lambda^2
    )

    tau^336 = zeros((no, no, nv, nv))

    tau^336 += einsum(
        "bckj,ikac->ijab", t^2, tau^335
    )

    tau^337 -= 3 * einsum(
        "klab,jkli->ijab", tau^336, tau^78
    )

    r^2 -= einsum(
        "ackj,kicb->abij", t^2, tau^337
    ) / 9

    del tau^337

    tau^382 += 12 * einsum(
        "jkab,ilab->ijkl", tau^144, tau^336
    )

    del tau^336

    tau^352 = zeros((no, no, nv, nv))

    tau^352 += einsum(
        "bckj,kica->ijab", t^2, tau^335
    )

    tau^353 -= 5 * einsum(
        "klab,kijl->ijab", tau^352, tau^78
    )

    del tau^352

    tau^375 = zeros((no, no, nv, nv))

    tau^375 += einsum(
        "jkbc,icka->ijab", tau^335, u
    )

    tau^377 += einsum(
        "jiba->ijab", tau^375
    )

    tau^394 = zeros((no, no, nv, nv))

    tau^394 += einsum(
        "jiba->ijab", tau^375
    )

    del tau^375

    tau^376 = zeros((no, no, nv, nv))

    tau^376 += einsum(
        "kjcb,kica->ijab", tau^10, tau^335
    )

    del tau^335

    tau^377 -= einsum(
        "ijab->ijab", tau^376
    )

    tau^382 -= 12 * einsum(
        "abjl,ikab->ijkl", t^2, tau^377
    )

    del tau^377

    tau^394 -= einsum(
        "ijab->ijab", tau^376
    )

    del tau^376

    tau^399 -= 12 * einsum(
        "bajl,ikab->ijkl", t^2, tau^394
    )

    del tau^394

    tau^338 = zeros((no, no, nv, nv))

    tau^338 -= einsum(
        "abij->ijab", t^2
    )

    tau^338 += 2 * einsum(
        "baij->ijab", t^2
    )

    tau^339 = zeros((no, no, nv, nv))

    tau^339 += einsum(
        "bcjk,ikca->ijab", \lambda^2, tau^338
    )

    tau^342 -= 9 * einsum(
        "acjk,ikbc->ijab", t^2, tau^339
    )

    tau^345 -= 9 * einsum(
        "jidc,bcda->ijab", tau^339, tau^65
    )

    del tau^339

    tau^344 = zeros((no, no, nv, nv))

    tau^344 += einsum(
        "caki,jkcb->ijab", \lambda^2, tau^338
    )

    tau^345 -= 9 * einsum(
        "ijcd,cbad->ijab", tau^344, u
    )

    del tau^344

    tau^412 -= einsum(
        "ca,jibc->ijab", tau^12, tau^338
    )

    del tau^12

    tau^414 += einsum(
        "jkab,kiab->ij", tau^412, u
    )

    del tau^412

    tau^414 -= einsum(
        "kjab,kiba->ij", tau^338, tau^404
    )

    del tau^338

    del tau^404

    tau^341 = zeros((no, no, nv, nv))

    tau^341 += 5 * einsum(
        "abij->ijab", t^2
    )

    tau^341 -= 2 * einsum(
        "abji->ijab", t^2
    )

    tau^342 += einsum(
        "klij,klab->ijab", tau^2, tau^341
    )

    r^2 += einsum(
        "jkbc,ikac->abij", tau^116, tau^342
    ) / 9

    del tau^116

    del tau^342

    tau^346 = zeros((nv, nv, nv, nv))

    tau^346 += einsum(
        "abij,ijcd->abcd", \lambda^2, tau^341
    )

    del tau^341

    tau^353 += 2 * einsum(
        "ijcd,acbd->ijab", tau^10, tau^346
    )

    del tau^10

    del tau^346

    tau^343 += einsum(
        "jabi->ijab", u
    )

    tau^343 -= 2 * einsum(
        "jaib->ijab", u
    )

    tau^345 -= 9 * einsum(
        "kibc,kjac->ijab", tau^343, tau^48
    )

    del tau^48

    del tau^343

    r^2 -= einsum(
        "bcjk,kica->abij", t^2, tau^345
    ) / 9

    del tau^345

    tau^350 = zeros((no, no, nv, nv))

    tau^350 -= einsum(
        "abij->ijab", t^2
    )

    tau^350 += 7 * einsum(
        "abji->ijab", t^2
    )

    tau^351 += einsum(
        "caki,kjbc->ijab", \lambda^2, tau^350
    )

    del tau^350

    tau^353 += 2 * einsum(
        "klab,kilj->ijab", tau^351, tau^78
    )

    del tau^351

    r^2 -= einsum(
        "bcki,jkca->abij", t^2, tau^353
    ) / 18

    del tau^353

    tau^355 = zeros((no, no, nv, nv))

    tau^355 += 2 * einsum(
        "abij->ijab", t^2
    )

    tau^355 += einsum(
        "baij->ijab", t^2
    )

    tau^356 -= einsum(
        "ca,ijcb->ijab", tau^89, tau^355
    )

    del tau^89

    del tau^355

    r^2 -= einsum(
        "ijcd,badc->abij", tau^356, u
    ) / 18

    del tau^356

    tau^358 = zeros((no, no, nv, nv))

    tau^358 += einsum(
        "acki,cbkj->ijab", \lambda^2, t^2
    )

    tau^359 = zeros((no, no, nv, nv))

    tau^359 += einsum(
        "ijcd,acbd->ijab", tau^358, tau^65
    )

    del tau^65

    del tau^358

    tau^365 -= einsum(
        "ijba->ijab", tau^359
    )

    tau^400 += einsum(
        "ijab->ijab", tau^359
    )

    del tau^359

    r^2 += einsum(
        "bcik,kjac->abij", t^2, tau^400
    ) / 18

    del tau^400

    tau^360 = zeros((no, no, nv, nv))

    tau^360 += 5 * einsum(
        "abij->ijab", \lambda^2
    )

    tau^360 -= 2 * einsum(
        "abji->ijab", \lambda^2
    )

    tau^361 = zeros((no, no, nv, nv))

    tau^361 += einsum(
        "cbkj,ikac->ijab", t^2, tau^360
    )

    del tau^360

    tau^365 += 2 * einsum(
        "kibc,kjac->ijab", tau^217, tau^361
    )

    del tau^217

    del tau^361

    tau^362 = zeros((no, no, nv, nv))

    tau^362 += 2 * einsum(
        "abij->ijab", \lambda^2
    )

    tau^362 += einsum(
        "abji->ijab", \lambda^2
    )

    tau^363 = zeros((no, no, nv, nv))

    tau^363 += einsum(
        "bckj,ikac->ijab", t^2, tau^362
    )

    tau^364 += einsum(
        "ijab->ijab", tau^363
    )

    tau^365 -= 6 * einsum(
        "klab,kjil->ijab", tau^364, tau^78
    )

    del tau^364

    r^2 -= einsum(
        "acki,kjcb->abij", t^2, tau^365
    ) / 18

    del tau^365

    tau^389 += einsum(
        "ijab->ijab", tau^363
    )

    del tau^363

    tau^399 += 12 * einsum(
        "jkab,ilab->ijkl", tau^144, tau^389
    )

    del tau^144

    del tau^389

    tau^368 += einsum(
        "cbjk,kica->ijab", t^2, tau^362
    )

    tau^374 += einsum(
        "ikac,jckb->ijab", tau^362, u
    )

    tau^395 += einsum(
        "bckj,kica->ijab", t^2, tau^362
    )

    del tau^362

    tau^399 -= 12 * einsum(
        "ilab,kabj->ijkl", tau^395, u
    )

    del tau^395

    tau^369 += einsum(
        "jabi->ijab", u
    )

    tau^382 -= 12 * einsum(
        "ijab,lkab->ijkl", tau^368, tau^369
    )

    del tau^368

    tau^399 -= 12 * einsum(
        "ijab,lkab->ijkl", tau^105, tau^369
    )

    del tau^105

    del tau^369

    tau^370 = zeros((no, no, nv, nv))

    tau^370 -= 2 * einsum(
        "jiab->ijab", u
    )

    tau^370 += 3 * einsum(
        "jiba->ijab", u
    )

    tau^371 = zeros((no, no, nv, nv))

    tau^371 += einsum(
        "bckj,kica->ijab", t^2, tau^370
    )

    del tau^370

    tau^372 += einsum(
        "jiba->ijab", tau^371
    )

    del tau^371

    tau^373 = zeros((no, no, nv, nv))

    tau^373 += einsum(
        "cbkj,kica->ijab", \lambda^2, tau^372
    )

    del tau^372

    tau^374 += einsum(
        "jiba->ijab", tau^373
    )

    tau^382 -= 12 * einsum(
        "bajl,ikab->ijkl", t^2, tau^374
    )

    del tau^374

    tau^391 -= einsum(
        "jiba->ijab", tau^373
    )

    del tau^373

    tau^379 = zeros((no, no, nv, nv))

    tau^379 += 3 * einsum(
        "iajb->ijab", u
    )

    tau^379 -= 2 * einsum(
        "iabj->ijab", u
    )

    tau^382 += 12 * einsum(
        "ilab,kjab->ijkl", tau^25, tau^379
    )

    del tau^25

    del tau^379

    r^2 += einsum(
        "ablk,kilj->abij", t^2, tau^382
    ) / 36

    del tau^382

    tau^383 = zeros((no, no, nv, nv))

    tau^383 += 2 * einsum(
        "abij->ijab", t^2
    )

    tau^383 += einsum(
        "abji->ijab", t^2
    )

    tau^385 = zeros((no, no, nv, nv))

    tau^385 += einsum(
        "caik,kjbc->ijab", \lambda^2, tau^383
    )

    del tau^383

    tau^384 = zeros((no, no, nv, nv))

    tau^384 += einsum(
        "abij->ijab", t^2
    )

    tau^384 += 2 * einsum(
        "abji->ijab", t^2
    )

    tau^385 += einsum(
        "caki,kjbc->ijab", \lambda^2, tau^384
    )

    del tau^384

    tau^388 += 2 * einsum(
        "klab,jkil->ijab", tau^385, tau^78
    )

    del tau^385

    tau^386 = zeros((no, no, nv, nv))

    tau^386 += 23 * einsum(
        "abij->ijab", \lambda^2
    )

    tau^386 += 10 * einsum(
        "abji->ijab", \lambda^2
    )

    tau^387 += einsum(
        "bckj,kica->ijab", t^2, tau^386
    )

    del tau^386

    tau^388 += einsum(
        "klab,jkli->ijab", tau^387, tau^78
    )

    del tau^387

    del tau^78

    r^2 += einsum(
        "bckj,kica->abij", t^2, tau^388
    ) / 36

    del tau^388

    tau^390 = zeros((no, no, nv, nv))

    tau^390 += 3 * einsum(
        "iabj->ijab", u
    )

    tau^390 -= 2 * einsum(
        "iajb->ijab", u
    )

    tau^391 += einsum(
        "acik,jkcb->ijab", \lambda^2, tau^390
    )

    del tau^390

    tau^399 += 12 * einsum(
        "abjl,ikab->ijkl", t^2, tau^391
    )

    del tau^391

    tau^392 = zeros((no, no, nv, nv))

    tau^392 += 3 * einsum(
        "jiab->ijab", u
    )

    tau^392 -= 2 * einsum(
        "jiba->ijab", u
    )

    tau^393 += einsum(
        "acki,kjcb->ijab", t^2, tau^392
    )

    del tau^392

    tau^393 += 2 * einsum(
        "jabi->ijab", u
    )

    tau^393 -= 3 * einsum(
        "jaib->ijab", u
    )

    tau^399 -= 12 * einsum(
        "lkab,ijab->ijkl", tau^393, tau^97
    )

    del tau^97

    del tau^393

    r^2 += einsum(
        "abkl,kilj->abij", t^2, tau^399
    ) / 36

    del tau^399

    tau^407 += 7 * einsum(
        "jikl->ijkl", u
    )

    tau^407 -= einsum(
        "jilk->ijkl", u
    )

    tau^411 += einsum(
        "lmik,kjlm->ij", tau^2, tau^407
    )

    del tau^407

    tau^409 = zeros((no, no, no, no))

    tau^409 -= einsum(
        "jikl->ijkl", u
    )

    tau^409 += 2 * einsum(
        "jilk->ijkl", u
    )

    tau^410 = zeros((no, no))

    tau^410 += einsum(
        "lk,kilj->ij", tau^14, tau^409
    )

    del tau^409

    del tau^14

    tau^411 -= 9 * einsum(
        "ji->ij", tau^410
    )

    r^2 -= einsum(
        "ik,abkj->abij", tau^411, t^2
    ) / 9

    del tau^411

    tau^414 -= einsum(
        "ij->ij", tau^410
    )

    del tau^410

    r^2 -= einsum(
        "kj,abik->abij", tau^414, t^2
    )

    del tau^414

    tau^415 += einsum(
        "jikl->ijkl", u
    )

    tau^415 += 2 * einsum(
        "jilk->ijkl", u
    )

    tau^416 = zeros((no, no))

    tau^416 += einsum(
        "lmki,kjlm->ij", tau^2, tau^415
    )

    del tau^2

    del tau^415

    r^2 += einsum(
        "jk,abki->abij", tau^416, t^2
    ) / 18

    del tau^416
