    tau^0 = zeros((nv, nv, nv, nv))

    tau^0 += einsum(
        "abji,cdji->abcd", \lambda^2, t^2
    )

    tau^106 = zeros((nv, nv, nv, nv))

    tau^106 -= einsum(
        "aefb,cedf->abcd", tau^0, u
    )

    tau^117 = zeros((nv, nv, nv, nv))

    tau^117 += 2 * einsum(
        "acbd->abcd", tau^106
    )

    del tau^106

    tau^130 = zeros((nv, nv, nv, nv))

    tau^130 += einsum(
        "afde,becf->abcd", tau^0, tau^0
    )

    tau^1 = zeros((no, no, nv, nv))

    tau^1 += einsum(
        "ci,jabc->ijab", t^1, u
    )

    tau^3 = zeros((no, no, nv, nv))

    tau^3 += einsum(
        "ijab->ijab", tau^1
    )

    tau^10 = zeros((no, no, nv, nv))

    tau^10 += einsum(
        "ijab->ijab", tau^1
    )

    tau^31 = zeros((no, no, nv, nv))

    tau^31 += einsum(
        "ijab->ijab", tau^1
    )

    tau^54 = zeros((no, no, nv, nv))

    tau^54 += einsum(
        "jiab->ijab", tau^1
    )

    tau^59 = zeros((no, no, nv, nv))

    tau^59 -= einsum(
        "jiab->ijab", tau^1
    )

    tau^113 = zeros((no, no, nv, nv))

    tau^113 += einsum(
        "ijab->ijab", tau^1
    )

    del tau^1

    tau^2 = zeros((no, no, nv, nv))

    tau^2 -= einsum(
        "caki,jkcb->ijab", t^2, u
    )

    tau^3 += einsum(
        "ijab->ijab", tau^2
    )

    tau^3 -= einsum(
        "jaib->ijab", u
    )

    tau^4 = zeros((no, no, nv, nv))

    tau^4 += einsum(
        "cadb,ijcd->ijab", tau^0, tau^3
    )

    tau^33 = zeros((no, no, nv, nv))

    tau^33 -= 2 * einsum(
        "ijab->ijab", tau^4
    )

    del tau^4

    tau^5 = zeros((nv, nv))

    tau^5 += einsum(
        "caji,cbji->ab", \lambda^2, t^2
    )

    tau^6 = zeros((no, no, nv, nv))

    tau^6 -= einsum(
        "ac,ijbc->ijab", tau^5, u
    )

    tau^17 = zeros((no, no, nv, nv))

    tau^17 -= 2 * einsum(
        "ijab->ijab", tau^6
    )

    tau^92 = zeros((no, no, nv, nv))

    tau^92 -= 4 * einsum(
        "ijba->ijab", tau^6
    )

    tau^119 = zeros((no, no, nv, nv))

    tau^119 -= einsum(
        "ijab->ijab", tau^6
    )

    tau^123 = zeros((no, no, nv, nv))

    tau^123 += 2 * einsum(
        "ijab->ijab", tau^6
    )

    del tau^6

    tau^26 = zeros((no, nv, nv, nv))

    tau^26 += einsum(
        "bi,ac->iabc", t^1, tau^5
    )

    tau^69 = zeros((no, no, nv, nv))

    tau^69 -= einsum(
        "cb,acji->ijab", tau^5, t^2
    )

    tau^70 = zeros((no, no, nv, nv))

    tau^70 += 2 * einsum(
        "ijab->ijab", tau^69
    )

    tau^133 = zeros((no, no, nv, nv))

    tau^133 += einsum(
        "ijab->ijab", tau^69
    )

    del tau^69

    tau^91 = zeros((no, no))

    tau^91 += einsum(
        "ab,iajb->ij", tau^5, u
    )

    tau^103 = zeros((no, no))

    tau^103 += 4 * einsum(
        "ji->ij", tau^91
    )

    del tau^91

    tau^99 = zeros((no, nv))

    tau^99 -= einsum(
        "bc,ibca->ia", tau^5, u
    )

    tau^101 = zeros((no, nv))

    tau^101 += einsum(
        "ia->ia", tau^99
    )

    del tau^99

    tau^122 = zeros((nv, nv))

    tau^122 -= einsum(
        "cd,cabd->ab", tau^5, u
    )

    tau^127 = zeros((nv, nv))

    tau^127 -= 4 * einsum(
        "ab->ab", tau^122
    )

    del tau^122

    tau^130 += einsum(
        "ac,bd->abcd", tau^5, tau^5
    )

    tau^7 = zeros((no, no, nv, nv))

    tau^7 += einsum(
        "ak,ikjb->ijab", t^1, u
    )

    tau^10 += einsum(
        "jiab->ijab", tau^7
    )

    tau^54 += einsum(
        "ijab->ijab", tau^7
    )

    del tau^7

    tau^8 = zeros((no, no, nv, nv))

    tau^8 += einsum(
        "baji->ijab", t^2
    )

    tau^8 -= einsum(
        "aj,bi->ijab", t^1, t^1
    )

    tau^9 = zeros((no, no, nv, nv))

    tau^9 += einsum(
        "kjcb,kica->ijab", tau^8, u
    )

    del tau^8

    tau^10 += einsum(
        "jiba->ijab", tau^9
    )

    tau^54 += einsum(
        "ijba->ijab", tau^9
    )

    del tau^9

    tau^10 -= einsum(
        "jaib->ijab", u
    )

    tau^11 = zeros((no, no, nv, nv))

    tau^11 += einsum(
        "caki,kjcb->ijab", \lambda^2, tau^10
    )

    tau^17 += 4 * einsum(
        "ijab->ijab", tau^11
    )

    tau^47 = zeros((no, no, nv, nv))

    tau^47 -= 4 * einsum(
        "ijab->ijab", tau^11
    )

    tau^76 = zeros((no, no, nv, nv))

    tau^76 += 4 * einsum(
        "ijab->ijab", tau^11
    )

    tau^92 -= 8 * einsum(
        "ijab->ijab", tau^11
    )

    tau^119 += 4 * einsum(
        "ijab->ijab", tau^11
    )

    tau^123 -= 8 * einsum(
        "ijab->ijab", tau^11
    )

    del tau^11

    tau^12 = zeros((no, no, no, no))

    tau^12 += einsum(
        "ai,jkla->ijkl", t^1, u
    )

    tau^15 = zeros((no, no, no, no))

    tau^15 -= 4 * einsum(
        "ljik->ijkl", tau^12
    )

    tau^29 = zeros((no, no, no, no))

    tau^29 -= 2 * einsum(
        "ljik->ijkl", tau^12
    )

    tau^82 = zeros((no, no, no, no))

    tau^82 -= einsum(
        "ljik->ijkl", tau^12
    )

    del tau^12

    tau^13 = zeros((no, no, nv, nv))

    tau^13 -= einsum(
        "baji->ijab", t^2
    )

    tau^13 += 2 * einsum(
        "aj,bi->ijab", t^1, t^1
    )

    tau^14 = zeros((no, no, no, no))

    tau^14 += einsum(
        "klab,ijab->ijkl", tau^13, u
    )

    del tau^13

    tau^15 -= einsum(
        "jilk->ijkl", tau^14
    )

    del tau^14

    tau^15 += 2 * einsum(
        "jilk->ijkl", u
    )

    tau^16 = zeros((no, no, nv, nv))

    tau^16 += einsum(
        "abkl,ijkl->ijab", \lambda^2, tau^15
    )

    del tau^15

    tau^17 += einsum(
        "jiba->ijab", tau^16
    )

    tau^18 = zeros((no, no, nv, nv))

    tau^18 += einsum(
        "caki,kjbc->ijab", t^2, tau^17
    )

    del tau^17

    tau^33 -= einsum(
        "ijba->ijab", tau^18
    )

    del tau^18

    tau^92 -= einsum(
        "jiba->ijab", tau^16
    )

    del tau^16

    tau^19 = zeros((no, no, nv, nv))

    tau^19 -= einsum(
        "acki,cbkj->ijab", \lambda^2, t^2
    )

    tau^20 = zeros((no, no, nv, nv))

    tau^20 += einsum(
        "kjbc,kiac->ijab", tau^10, tau^19
    )

    del tau^10

    tau^33 += 4 * einsum(
        "ijab->ijab", tau^20
    )

    del tau^20

    tau^25 = zeros((no, nv, nv, nv))

    tau^25 += einsum(
        "bj,jiac->iabc", t^1, tau^19
    )

    tau^26 += 2 * einsum(
        "iacb->iabc", tau^25
    )

    del tau^25

    tau^53 = zeros((no, no, nv, nv))

    tau^53 += einsum(
        "ikca,jkcb->ijab", tau^19, tau^3
    )

    tau^61 = zeros((no, no, nv, nv))

    tau^61 += 4 * einsum(
        "ijab->ijab", tau^53
    )

    del tau^53

    tau^63 = zeros((no, no, nv, nv))

    tau^63 += einsum(
        "acbd,ijdc->ijab", tau^0, tau^19
    )

    tau^72 = zeros((no, no, nv, nv))

    tau^72 += 2 * einsum(
        "ijab->ijab", tau^63
    )

    del tau^63

    tau^65 = zeros((no, no, nv, nv))

    tau^65 += einsum(
        "ikcb,kjac->ijab", tau^19, tau^19
    )

    tau^72 -= 4 * einsum(
        "ijab->ijab", tau^65
    )

    del tau^65

    tau^67 = zeros((no, no, nv, nv))

    tau^67 += einsum(
        "caki,kjcb->ijab", t^2, tau^19
    )

    tau^70 += 4 * einsum(
        "ijba->ijab", tau^67
    )

    tau^131 = zeros((no, no, nv, nv))

    tau^131 += 2 * einsum(
        "ijab->ijab", tau^67
    )

    tau^133 += 2 * einsum(
        "ijba->ijab", tau^67
    )

    del tau^67

    tau^134 = zeros((no, no, no, no))

    tau^134 += einsum(
        "ijab,lkab->ijkl", tau^133, u
    )

    del tau^133

    r^2 = zeros((nv, nv, no, no))

    r^2 -= einsum(
        "bakl,klij->abij", \lambda^2, tau^134
    ) / 4

    del tau^134

    tau^78 = zeros((no, no, no, no))

    tau^78 += einsum(
        "ijab,klab->ijkl", tau^19, tau^3
    )

    del tau^3

    tau^88 = zeros((no, no, no, no))

    tau^88 += 8 * einsum(
        "ijlk->ijkl", tau^78
    )

    del tau^78

    tau^107 = zeros((nv, nv, nv, nv))

    tau^107 += einsum(
        "ijab,ijcd->abcd", tau^19, tau^2
    )

    del tau^2

    tau^117 += 4 * einsum(
        "acbd->abcd", tau^107
    )

    del tau^107

    tau^130 += 4 * einsum(
        "ijad,jibc->abcd", tau^19, tau^19
    )

    r^2 -= einsum(
        "abcd,jicd->abij", tau^130, u
    ) / 4

    del tau^130

    tau^132 = zeros((no, no, no, no))

    tau^132 += 4 * einsum(
        "ikba,jlab->ijkl", tau^19, tau^19
    )

    tau^21 = zeros((no, no, no, nv))

    tau^21 += einsum(
        "bi,jkab->ijka", t^1, u
    )

    tau^22 = zeros((no, no, no, nv))

    tau^22 += einsum(
        "kjia->ijka", tau^21
    )

    tau^57 = zeros((no, no, no, nv))

    tau^57 -= einsum(
        "ikja->ijka", tau^21
    )

    tau^84 = zeros((no, no, no, nv))

    tau^84 += einsum(
        "kjia->ijka", tau^21
    )

    del tau^21

    tau^22 -= einsum(
        "jika->ijka", u
    )

    tau^23 = zeros((no, no, no, nv))

    tau^23 += einsum(
        "bk,abij->ijka", t^1, \lambda^2
    )

    tau^24 = zeros((no, nv, nv, nv))

    tau^24 += einsum(
        "bckj,kjia->iabc", t^2, tau^23
    )

    tau^26 -= einsum(
        "iacb->iabc", tau^24
    )

    del tau^24

    tau^27 = zeros((no, no, nv, nv))

    tau^27 += einsum(
        "kijc,kabc->ijab", tau^22, tau^26
    )

    del tau^26

    tau^33 += 2 * einsum(
        "jiab->ijab", tau^27
    )

    del tau^27

    tau^35 = zeros((no, no, no, nv))

    tau^35 -= einsum(
        "balk,iljb->ijka", t^2, tau^23
    )

    tau^36 = zeros((no, no, no, nv))

    tau^36 -= einsum(
        "iljb,klab->ijka", tau^35, u
    )

    tau^37 = zeros((no, no, nv, nv))

    tau^37 += einsum(
        "ak,ijkb->ijab", t^1, tau^36
    )

    del tau^36

    tau^61 += 4 * einsum(
        "ijab->ijab", tau^37
    )

    del tau^37

    tau^56 = zeros((no, no, no, nv))

    tau^56 += 2 * einsum(
        "ijka->ijka", tau^35
    )

    tau^80 = zeros((no, no, no, nv))

    tau^80 += 2 * einsum(
        "ijka->ijka", tau^35
    )

    del tau^35

    tau^95 = zeros((no, nv))

    tau^95 += einsum(
        "bakj,kjib->ia", t^2, tau^23
    )

    del tau^23

    tau^96 = zeros((no, nv))

    tau^96 += einsum(
        "ia->ia", tau^95
    )

    del tau^95

    tau^28 = zeros((no, no, no, no))

    tau^28 += einsum(
        "baij,klba->ijkl", t^2, u
    )

    tau^29 += einsum(
        "lkji->ijkl", tau^28
    )

    tau^29 += 2 * einsum(
        "jilk->ijkl", u
    )

    tau^30 = zeros((no, no, nv, nv))

    tau^30 += einsum(
        "klab,likj->ijab", tau^19, tau^29
    )

    del tau^29

    tau^33 -= 2 * einsum(
        "jiab->ijab", tau^30
    )

    del tau^30

    tau^31 -= einsum(
        "jaib->ijab", u
    )

    tau^32 = zeros((no, no, nv, nv))

    tau^32 += einsum(
        "ac,ijbc->ijab", tau^5, tau^31
    )

    del tau^31

    tau^33 -= 2 * einsum(
        "ijab->ijab", tau^32
    )

    del tau^32

    tau^34 = zeros((no, no, nv, nv))

    tau^34 += einsum(
        "caki,kjbc->ijab", \lambda^2, tau^33
    )

    del tau^33

    tau^74 = zeros((no, no, nv, nv))

    tau^74 -= einsum(
        "ijba->ijab", tau^34
    )

    del tau^34

    tau^38 = zeros((no, no, no, no))

    tau^38 += einsum(
        "baij,bakl->ijkl", \lambda^2, t^2
    )

    tau^39 = zeros((no, no, nv, nv))

    tau^39 -= einsum(
        "jilk,lkab->ijab", tau^38, u
    )

    tau^47 += einsum(
        "ijba->ijab", tau^39
    )

    tau^123 += einsum(
        "ijba->ijab", tau^39
    )

    del tau^39

    tau^64 = zeros((no, no, nv, nv))

    tau^64 += einsum(
        "klab,iljk->ijab", tau^19, tau^38
    )

    tau^72 += 2 * einsum(
        "ijab->ijab", tau^64
    )

    del tau^64

    tau^66 = zeros((no, no, nv, nv))

    tau^66 -= einsum(
        "ablk,lkji->ijab", t^2, tau^38
    )

    tau^70 += einsum(
        "ijba->ijab", tau^66
    )

    del tau^66

    tau^75 = zeros((no, no, no, no))

    tau^75 += einsum(
        "mjln,imnk->ijkl", tau^28, tau^38
    )

    del tau^28

    tau^88 -= 2 * einsum(
        "ijlk->ijkl", tau^75
    )

    del tau^75

    tau^79 = zeros((no, no, no, nv))

    tau^79 -= einsum(
        "al,ilkj->ijka", t^1, tau^38
    )

    tau^80 += einsum(
        "ikja->ijka", tau^79
    )

    del tau^79

    tau^81 = zeros((no, no, no, no))

    tau^81 += einsum(
        "mija,kmla->ijkl", tau^22, tau^80
    )

    del tau^80

    del tau^22

    tau^88 -= 4 * einsum(
        "klij->ijkl", tau^81
    )

    del tau^81

    tau^120 = zeros((no, no, nv, nv))

    tau^120 += einsum(
        "klab,ijlk->ijab", tau^119, tau^38
    )

    del tau^119

    tau^129 = zeros((no, no, nv, nv))

    tau^129 -= einsum(
        "jiab->ijab", tau^120
    )

    del tau^120

    tau^132 += einsum(
        "inkm,jmln->ijkl", tau^38, tau^38
    )

    tau^40 = zeros((no, no))

    tau^40 += einsum(
        "baki,bakj->ij", \lambda^2, t^2
    )

    tau^41 = zeros((no, no, nv, nv))

    tau^41 -= einsum(
        "ik,jkab->ijab", tau^40, u
    )

    tau^47 -= 2 * einsum(
        "ijba->ijab", tau^41
    )

    tau^76 += einsum(
        "ijba->ijab", tau^41
    )

    tau^77 = zeros((no, no, no, no))

    tau^77 += einsum(
        "abij,klba->ijkl", t^2, tau^76
    )

    del tau^76

    tau^88 -= einsum(
        "ljik->ijkl", tau^77
    )

    del tau^77

    tau^92 -= 2 * einsum(
        "ijba->ijab", tau^41
    )

    tau^93 = zeros((no, no))

    tau^93 += einsum(
        "abki,kjab->ij", t^2, tau^92
    )

    del tau^92

    tau^103 -= einsum(
        "ij->ij", tau^93
    )

    del tau^93

    tau^123 += 4 * einsum(
        "jiba->ijab", tau^41
    )

    del tau^41

    tau^56 -= einsum(
        "aj,ik->ijka", t^1, tau^40
    )

    tau^68 = zeros((no, no, nv, nv))

    tau^68 += einsum(
        "kj,abik->ijab", tau^40, t^2
    )

    tau^70 -= 2 * einsum(
        "ijba->ijab", tau^68
    )

    tau^71 = zeros((no, no, nv, nv))

    tau^71 += einsum(
        "caki,kjcb->ijab", \lambda^2, tau^70
    )

    del tau^70

    tau^72 += einsum(
        "ijab->ijab", tau^71
    )

    del tau^71

    tau^131 += einsum(
        "ijba->ijab", tau^68
    )

    del tau^68

    tau^132 += einsum(
        "abji,klab->ijkl", \lambda^2, tau^131
    )

    del tau^131

    tau^72 -= einsum(
        "ij,ab->ijab", tau^40, tau^5
    )

    tau^73 = zeros((no, no, nv, nv))

    tau^73 += einsum(
        "jkbc,kica->ijab", tau^72, u
    )

    del tau^72

    tau^74 -= einsum(
        "jiba->ijab", tau^73
    )

    del tau^73

    tau^90 = zeros((no, no))

    tau^90 -= einsum(
        "kl,ilkj->ij", tau^40, u
    )

    tau^103 -= 4 * einsum(
        "ji->ij", tau^90
    )

    del tau^90

    tau^94 = zeros((no, nv))

    tau^94 += einsum(
        "aj,ji->ia", t^1, tau^40
    )

    tau^96 += einsum(
        "ia->ia", tau^94
    )

    del tau^94

    tau^97 = zeros((no, no))

    tau^97 += einsum(
        "ka,kija->ij", tau^96, u
    )

    tau^103 += 4 * einsum(
        "ji->ij", tau^97
    )

    del tau^97

    tau^100 = zeros((no, nv))

    tau^100 += einsum(
        "jb,jiba->ia", tau^96, u
    )

    tau^101 -= einsum(
        "ia->ia", tau^100
    )

    del tau^100

    tau^125 = zeros((nv, nv))

    tau^125 += einsum(
        "ic,iacb->ab", tau^96, u
    )

    del tau^96

    tau^127 += 4 * einsum(
        "ab->ab", tau^125
    )

    del tau^125

    tau^98 = zeros((no, nv))

    tau^98 += einsum(
        "jk,ikja->ia", tau^40, u
    )

    tau^101 += einsum(
        "ia->ia", tau^98
    )

    del tau^98

    tau^102 = zeros((no, no))

    tau^102 += einsum(
        "aj,ia->ij", t^1, tau^101
    )

    tau^103 += 4 * einsum(
        "ji->ij", tau^102
    )

    del tau^102

    tau^104 = zeros((no, no, nv, nv))

    tau^104 += einsum(
        "kj,abki->ijab", tau^103, \lambda^2
    )

    del tau^103

    tau^105 = zeros((no, no, nv, nv))

    tau^105 += einsum(
        "ijba->ijab", tau^104
    )

    del tau^104

    tau^126 = zeros((nv, nv))

    tau^126 += einsum(
        "bi,ia->ab", t^1, tau^101
    )

    del tau^101

    tau^127 += 4 * einsum(
        "ba->ab", tau^126
    )

    del tau^126

    tau^121 = zeros((nv, nv))

    tau^121 += einsum(
        "ij,jaib->ab", tau^40, u
    )

    tau^127 += 4 * einsum(
        "ab->ab", tau^121
    )

    del tau^121

    tau^132 -= einsum(
        "ik,jl->ijkl", tau^40, tau^40
    )

    r^2 += einsum(
        "ijkl,klba->abij", tau^132, u
    ) / 4

    del tau^132

    tau^42 = zeros((no, nv, nv, nv))

    tau^42 -= einsum(
        "aj,jibc->iabc", t^1, u
    )

    tau^43 = zeros((no, nv, nv, nv))

    tau^43 += einsum(
        "iacb->iabc", tau^42
    )

    del tau^42

    tau^43 -= 2 * einsum(
        "iacb->iabc", u
    )

    tau^44 = zeros((nv, nv, nv, nv))

    tau^44 += einsum(
        "ai,ibcd->abcd", t^1, tau^43
    )

    del tau^43

    tau^45 = zeros((nv, nv, nv, nv))

    tau^45 -= einsum(
        "badc->abcd", tau^44
    )

    tau^115 = zeros((nv, nv, nv, nv))

    tau^115 += einsum(
        "abdc->abcd", tau^44
    )

    del tau^44

    tau^45 += einsum(
        "badc->abcd", u
    )

    tau^46 = zeros((no, no, nv, nv))

    tau^46 += einsum(
        "cdij,cdab->ijab", \lambda^2, tau^45
    )

    del tau^45

    tau^47 -= 2 * einsum(
        "jiba->ijab", tau^46
    )

    tau^48 = zeros((no, no, nv, nv))

    tau^48 += einsum(
        "caki,jkcb->ijab", t^2, tau^47
    )

    del tau^47

    tau^61 += einsum(
        "jiab->ijab", tau^48
    )

    del tau^48

    tau^123 -= 2 * einsum(
        "jiba->ijab", tau^46
    )

    del tau^46

    tau^124 = zeros((nv, nv))

    tau^124 += einsum(
        "cbij,ijca->ab", t^2, tau^123
    )

    del tau^123

    tau^127 -= einsum(
        "ba->ab", tau^124
    )

    del tau^124

    tau^128 = zeros((no, no, nv, nv))

    tau^128 += einsum(
        "ca,cbij->ijab", tau^127, \lambda^2
    )

    del tau^127

    tau^129 += einsum(
        "jiba->ijab", tau^128
    )

    del tau^128

    tau^49 = zeros((nv, nv, nv, nv))

    tau^49 += einsum(
        "abji,jicd->abcd", t^2, u
    )

    tau^51 = zeros((nv, nv, nv, nv))

    tau^51 += einsum(
        "badc->abcd", tau^49
    )

    del tau^49

    tau^50 = zeros((nv, nv, nv, nv))

    tau^50 += einsum(
        "ai,ibcd->abcd", t^1, u
    )

    tau^51 += 2 * einsum(
        "abdc->abcd", tau^50
    )

    tau^51 -= 2 * einsum(
        "badc->abcd", tau^50
    )

    tau^110 = zeros((nv, nv, nv, nv))

    tau^110 += 2 * einsum(
        "abdc->abcd", tau^50
    )

    tau^110 -= 2 * einsum(
        "badc->abcd", tau^50
    )

    del tau^50

    tau^51 += 2 * einsum(
        "badc->abcd", u
    )

    tau^52 = zeros((no, no, nv, nv))

    tau^52 += einsum(
        "ijcd,cadb->ijab", tau^19, tau^51
    )

    del tau^51

    tau^61 -= 2 * einsum(
        "ijab->ijab", tau^52
    )

    del tau^52

    tau^54 -= einsum(
        "iajb->ijab", u
    )

    tau^55 = zeros((no, no, nv, nv))

    tau^55 += einsum(
        "kilj,lkab->ijab", tau^38, tau^54
    )

    del tau^54

    tau^61 -= 2 * einsum(
        "ijab->ijab", tau^55
    )

    del tau^55

    tau^57 += einsum(
        "kjia->ijka", u
    )

    tau^58 = zeros((no, no, nv, nv))

    tau^58 += einsum(
        "ikla,jlkb->ijab", tau^56, tau^57
    )

    del tau^56

    tau^61 -= 2 * einsum(
        "ijab->ijab", tau^58
    )

    del tau^58

    tau^112 = zeros((no, no, nv, nv))

    tau^112 += einsum(
        "ak,ikjb->ijab", t^1, tau^57
    )

    del tau^57

    tau^113 += einsum(
        "ijab->ijab", tau^112
    )

    del tau^112

    tau^59 += einsum(
        "iajb->ijab", u
    )

    tau^60 = zeros((no, no, nv, nv))

    tau^60 += einsum(
        "ik,kjab->ijab", tau^40, tau^59
    )

    del tau^59

    tau^61 += 2 * einsum(
        "ijab->ijab", tau^60
    )

    del tau^60

    tau^62 = zeros((no, no, nv, nv))

    tau^62 += einsum(
        "caki,jkcb->ijab", \lambda^2, tau^61
    )

    del tau^61

    tau^74 -= einsum(
        "jiab->ijab", tau^62
    )

    del tau^62

    r^2 += einsum(
        "ijab->abij", tau^74
    ) / 4

    r^2 -= einsum(
        "ijba->abij", tau^74
    ) / 4

    r^2 -= einsum(
        "jiab->abij", tau^74
    ) / 4

    r^2 += einsum(
        "jiba->abij", tau^74
    ) / 4

    del tau^74

    tau^82 += einsum(
        "jilk->ijkl", u
    )

    tau^83 = zeros((no, no, no, no))

    tau^83 += einsum(
        "minj,nkml->ijkl", tau^38, tau^82
    )

    del tau^82

    del tau^38

    tau^88 += 4 * einsum(
        "ijkl->ijkl", tau^83
    )

    del tau^83

    tau^84 -= 2 * einsum(
        "jika->ijka", u
    )

    tau^85 = zeros((no, no, no, no))

    tau^85 += einsum(
        "ai,jkla->ijkl", t^1, tau^84
    )

    del tau^84

    tau^86 = zeros((no, no, no, no))

    tau^86 -= einsum(
        "kjil->ijkl", tau^85
    )

    del tau^85

    tau^86 -= einsum(
        "jilk->ijkl", u
    )

    tau^87 = zeros((no, no, no, no))

    tau^87 += einsum(
        "im,mjkl->ijkl", tau^40, tau^86
    )

    del tau^40

    del tau^86

    tau^88 += 2 * einsum(
        "iklj->ijkl", tau^87
    )

    del tau^87

    tau^89 = zeros((no, no, nv, nv))

    tau^89 += einsum(
        "abkl,ikjl->ijab", \lambda^2, tau^88
    )

    del tau^88

    tau^105 -= einsum(
        "ijba->ijab", tau^89
    )

    del tau^89

    r^2 += einsum(
        "ijba->abij", tau^105
    ) / 8

    r^2 -= einsum(
        "jiba->abij", tau^105
    ) / 8

    del tau^105

    tau^108 = zeros((no, no, nv, nv))

    tau^108 += einsum(
        "baji->ijab", t^2
    )

    tau^108 += 2 * einsum(
        "ai,bj->ijab", t^1, t^1
    )

    tau^109 = zeros((nv, nv, nv, nv))

    tau^109 += einsum(
        "ijab,ijcd->abcd", tau^108, u
    )

    del tau^108

    tau^110 += einsum(
        "badc->abcd", tau^109
    )

    del tau^109

    tau^111 = zeros((nv, nv, nv, nv))

    tau^111 += einsum(
        "ecfd,eafb->abcd", tau^0, tau^110
    )

    del tau^0

    del tau^110

    tau^117 -= einsum(
        "cdab->abcd", tau^111
    )

    del tau^111

    tau^113 -= einsum(
        "jaib->ijab", u
    )

    tau^114 = zeros((nv, nv, nv, nv))

    tau^114 += einsum(
        "ijab,ijcd->abcd", tau^113, tau^19
    )

    del tau^113

    del tau^19

    tau^117 -= 4 * einsum(
        "cdab->abcd", tau^114
    )

    del tau^114

    tau^115 += einsum(
        "badc->abcd", u
    )

    tau^116 = zeros((nv, nv, nv, nv))

    tau^116 += einsum(
        "de,abec->abcd", tau^5, tau^115
    )

    del tau^115

    del tau^5

    tau^117 += einsum(
        "cbda->abcd", tau^116
    )

    del tau^116

    tau^118 = zeros((no, no, nv, nv))

    tau^118 += einsum(
        "cdij,acdb->ijab", \lambda^2, tau^117
    )

    del tau^117

    tau^129 += 2 * einsum(
        "jiab->ijab", tau^118
    )

    del tau^118

    r^2 += einsum(
        "jiab->abij", tau^129
    ) / 8

    r^2 -= einsum(
        "jiba->abij", tau^129
    ) / 8

    del tau^129
